# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Security
- **Self-registered trial accounts now actually expire.** On a deployment with the trial system enabled, the landing page's register form marked new users as 14-day trials by setting flags on the `User` alone — but the hourly expiry sweep only ever queried `DemoApplication` records, which registration never created. The result: the waitlist path expired on schedule while the plain register form handed out unlimited free access, bypassing the survey, the 50-active cap, and the per-org cap at once. Registration now goes through `demo_service.begin_self_serve_trial`, which mints an *active* `DemoApplication` (or adopts a pending waitlist application with the same email, so one person never holds two records) alongside the user flags — putting self-registered users on exactly the same rails as waitlist activations: the expiry sweep, the 2-day warning email, the trial-end/renewal screen, engagement scoring, and the admin dashboard. Existing flag-only accounts are backfilled by the sweep itself: each gets an application carrying its original expiry, so an already-overdue account is locked in the same pass that first sees it. Self-serve applications record the signup's email domain as the organization, so per-domain concentration is at least visible in the admin stats.
- **Trial accounts carry a hard LLM spend cap.** A trial signup requires no email verification, and nothing anywhere limited how many tokens a trial user could burn — an unverified stranger could run unbounded spend on the deployment's API keys. Trial users (`is_demo_user`) now have a lifetime token budget, `TRIAL_TOKEN_BUDGET` (default 5,000,000 input+output tokens; 0 disables), enforced against the `llm_usage` ledger at metering-scope entry — the one chokepoint every attributed LLM operation already passes through, in both the API process and the Celery workers, so chat, extractions, workflows, validation, and background passive tasks are all covered by a single check rather than a per-feature patchwork. An exhausted budget refuses the operation *before* its first model request: HTTP surfaces return 402 with a plain-language message, and chat streams the same message as an in-conversation notice instead of a broken stream. The check costs nothing on deployments without the trial system (it gates on `ENABLE_TRIAL_SYSTEM`), never limits regular accounts, and fails open on its own infrastructure errors — only the budget-exceeded signal itself blocks anything.

### Added
- **A shareable synthetic benchmark corpus, with its answer keys validated in CI.** The only realistic test material for this product is real grant proposals, which cannot be committed, quoted, or shared — so every accuracy measurement reported against them was unreproducible by anyone else, and the project had no document-level regression fixture with known-correct answers (the judge-calibration fixtures cover scoring, not ingestion, retrieval, and citation over real documents). `benchmarks/corpus/CSU-NSF-001` adds one: a fictional three-year NSF submission (16 documents in DOCX/PDF plus three workbooks — a formula-driven budget workbook and a collaborators-and-affiliations workbook for each investigator, 42 pages), with 30 questions whose answers and per-document page citations ship in-tree, and every document additionally rasterized at three degradation severities with no text layer, so the OCR path is genuinely forced rather than quietly bypassed. The documents themselves are release assets pinned by sha256 in the corpus manifest — they stay out of the tree deliberately, both because rasterized binaries would bloat every clone permanently and because searchable synthetic proposals are exactly what scholarly search engines index. What ships in-tree is what needs review and diffing: README, manifest, the blind question set, the answer key, the validators, and the benchmark harness and two scorers described below. The key gains a `corroborating_sources` field, carried by all 30 questions and populated on the 19 whose decisive fact is also stated on a page outside the minimal canonical set, so automated citation scoring accepts a true citation to one of those pages instead of punishing it. Those validators now run in CI on every change: structural key checks and a person-name scan over the tree on every PR, and on every corpus release a hash-verified pass over the actual binaries — the name scan matters most, because the corpus's identity-safety policy exists after early synthetic identities collided with real, findable people, and a denylist can only catch names someone already knows about. The corpus README states plainly what results from it cannot claim: the scanned variants are synthetic degradation rather than scanner output, latency measured on a shared GPU host is scheduler noise, and the corpus detects breakage without ranking good configurations. It also carries the first end-to-end measurement of itself — 900 answers over the product's own chat API, five models, two modes, three repeats — which is what retires the older claim that the packet is too small to exercise context limits: the assembled 16-document packet is 31,881–31,900 tokens against a 24,576-token input budget, so long-document routing fired on every request from four of the five models, and the corpus cannot compare models in attached-document mode at all, because all five columns end up served by the same one. The harness that produced that measurement ships with the corpus, alongside the two scorers that graded it, so the tables are re-runnable against any deployment rather than taken on trust; no workflow invokes any of the three, because asking the questions needs a live instance and real GPU time — the same footing as the tier-3 `INTEGRATION_LLM` suite, which runs on a schedule rather than on every pull request — but their unit tests do run in CI, on every change, alongside the validators'. An external sponsor-policy review of the first cut then found five places where the packet modelled sponsor requirements that have since been retired — most consequentially the MTDC subaward threshold, which 2 CFR 200.1 has raised to the first $50,000 of each subaward — so what ships is v0.5.0, with all five corrected against PAPPG 24-1 and the 2024 Uniform Guidance, the total request recomputed to $1,184,398.51, per-person senior-personnel documents in place of the retired combined ones, and three policy invariants added to the release validator so none of those rules can quietly drift back. (#628)
- **A knowledge-base source citation can now open the document, not just preview the chunk.** Clicking a source under a chat answer expanded the retrieved chunk's text and nothing else, so checking whether the cited page was right meant leaving the answer, finding the document in the file browser, opening it, and paging to the number by hand. That is the one thing a reader most needs to do here, because the page on a citation is a retrieval heuristic and is not always correct — and a preview of the chunk cannot confirm or deny it, since the chunk is the very thing whose location is in question. Clicking a source now offers **Preview**, which shows the chunk text exactly as before, and **Open at p. N**, which opens the document in the file viewer, scrolled to and highlighting the cited passage. The passage is the primary target and the page only breaks ties between matches, so a wrong page number lands the reader on the real text instead of the wrong page — and when the passage cannot be matched at all, the viewer still jumps to the cited page and says so, which is itself the answer to "is this page right?". Once a document is open, its other chunks drop the chooser and go straight to preview: the click that would re-open what is already on screen is not worth the extra step. Sources with nothing to open — URL-backed knowledge-base sources, and documents that have since been deleted — keep today's single-click preview rather than offering an Open that would dead-end; the backend resolves the underlying document per citation and omits it when it cannot be read, so the UI never offers a link it cannot honour. Citations stored before this change carry no document reference and stay preview-only until the conversation is asked again.
- **Bulk deletion and author filtering for knowledge-base test queries.** A KB that has been imported from a spreadsheet or auto-generated a few times accumulates hundreds of test queries, and the Test Queries tab only ever offered a per-row trash icon — pruning a stale generated batch meant several hundred individual confirms, which in practice meant nobody pruned. The tab now has per-row checkboxes, a **Select all** control, and a **Delete selected (n)** button behind a single confirmation, plus an **All / User-authored / Auto-generated** filter with live counts. The filter and the selection are deliberately coupled: **Select all** takes the slice currently on screen, so "drop everything the generator produced and keep what I wrote" is two clicks and one confirm. Selections survive switching filters, so a batch can be gathered from more than one slice before deleting. A new `POST /{uuid}/test-queries/bulk-delete` endpoint does the removal in one query, scoped to the KB and behind the same manage gate as single-row delete — view-only evaluators keep the filter (it is read-only) and get no checkboxes. Ids that no longer exist are skipped rather than failing the batch, so a list left open in another tab still deletes what it legitimately can, and the response reports how many rows were actually removed. Batches are capped at 2000. Past runs keep the scores and answers they recorded, but a validation run re-exported afterwards has a blank expected answer for any question deleted here — the export re-joins the live test-query set, and `ValidationRun` carries no snapshot of its own. The confirmation says so before you commit to it.- **Container images, secrets, and Dockerfile config are now scanned.** `bandit`, `pip-audit`, and `npm audit` between them covered application code and the two dependency lockfiles, but nothing looked inside the runtime container images — where the highest-severity findings usually live. `make security` runs Trivy across both lockfiles, the runtime base images, committed secrets, and Dockerfile misconfiguration; `make security-gate` is the release gate and joins `make release-check`. The gate fails on a **fixable** CRITICAL or a leaked secret — both zero today, so it is enforceable immediately — and deliberately ignores unfixed findings, because a gate nobody can make pass is a gate that gets bypassed. `python:3.12-slim` currently carries four CRITICAL `perl-base` CVEs with no Debian fix available; they are reported, not gated on. The images we publish to ghcr.io are scanned too, not just their base layers — that is the artifact a deploying campus actually pulls, and it is the only thing that sees the Chromium apt tree `playwright install --with-deps` bakes into the backend image, which appears in neither `uv.lock` nor the base image. HIGH findings stay advisory but are now printed in their own CI job rather than being silently swallowed by `npm audit --audit-level=critical` and a non-blocking pip-audit target — which is how 35 of them accumulated unnoticed.
- **Pending reviews are reachable from the interface, not just from a notification.** The approval queue has existed at `/reviews` since approvals shipped, and nothing in the app linked to it: no menu entry, no sidebar item, no count anywhere. The only ways in were clicking a notification link or typing the URL, so a reviewer who missed the notification, dismissed it, or was assigned an approval while away had no way to find the queue and no way to learn it existed — while the run sat blocked. **Reviews** is now a permanent item in the account menu, listed even at zero so the surface is discoverable, and the activity rail grows a badged **Reviews** entry with the pending count whenever something is actually waiting. Both read the same 30s poll of the already-existing (and previously unused) `/api/reviews/count`, shared at module scope so the two badges cost one request; deciding a review from either the full page or the workflow panel re-polls immediately rather than leaving a stale count for up to half a minute. A paused run now links to the review that is blocking it, from both directions it can be looked at: the activity rail relabels the row **Awaiting approval** and opens the review on click, and the workflow's Run history row shows an `awaiting approval` status with an **Open review →** link. This is carried by a new `meta_summary.pending_review_uuid` marker stamped on the run's activity when it pauses and cleared by every path out of the wait (resume, reject, timeout), rather than a new activity status, which would have meant touching every consumer of the rail. `/reviews` and `/reviews/:uuid` also now render inside the standard page layout — landing on either from a notification previously dropped the user on a bare page with no header, no account menu, and no notification bell.
- **Container images, secrets, and Dockerfile config are now scanned.** `bandit`, `pip-audit`, and `npm audit` between them covered application code and the two dependency lockfiles, but nothing looked inside the runtime container images — where the highest-severity findings usually live. `make security` runs Trivy across both lockfiles, the runtime base images, committed secrets, and Dockerfile misconfiguration; `make security-gate` is the release gate and joins `make release-check`. The gate fails on a **fixable** CRITICAL or a leaked secret — both zero today, so it is enforceable immediately — and deliberately ignores unfixed findings, because a gate nobody can make pass is a gate that gets bypassed. `python:3.12-slim` currently carries four CRITICAL `perl-base` CVEs with no Debian fix available; they are reported, not gated on. The images we publish to ghcr.io are scanned too, not just their base layers — that is the artifact a deploying campus actually pulls, and it is the only thing that sees the Chromium apt tree `playwright install --with-deps` bakes into the backend image, which appears in neither `uv.lock` nor the base image. HIGH findings stay advisory but are now printed in their own CI job rather than being silently swallowed by `npm audit --audit-level=critical` and a non-blocking pip-audit target — which is how 35 of them accumulated unnoticed.- **Document chat can now cite the page a fact came from.** Page structure was already computed at ingest and stored on the document (`SmartDocument.text_markers`, one `{char_offset, kind: "page", value: n}` per page), and both extraction sources and knowledge-base chat already resolved it — but document chat discarded it, sending `raw_text` as one flat block. The model therefore had no way to attribute a claim to a page, even though KB chat on the same screen renders "(p. 12)". Document context is now annotated with `[p. N]` at each page boundary, with a short note telling the model it may cite pages; the note is omitted for documents that have no page structure (DOCX and other unpaginated formats, and PDFs ingested before page counts were persisted) so the model isn't invited to invent page numbers it can't see. Text content is passed through unmodified apart from the inserted markers, and markers whose offsets no longer fall inside the stored text — possible if `raw_text` was later re-saved shorter — are dropped rather than mangling the body. Sheet markers from XLSX are ignored, since a sheet is not a page. Scanned PDFs are handled differently on purpose: their text comes from the OCR endpoint, which doesn't preserve page structure, so boundaries are interpolated by spreading the known page count evenly across the text (`_interpolate_page_markers`). Those estimates were previously indistinguishable from measured boundaries, which would have let the model cite an interpolated offset as though it were exact. Interpolated markers now carry `approximate: true`, render as `[p. ~N]`, and get a note telling the model to cite them as approximate ("around p. 4"). Markers persisted before this flag existed carry no key and are treated as exact, matching current behaviour until those documents are reprocessed. (#603)
- **A per-model sampling temperature, settable by an admin.** The shared settings builder never sent a temperature, so every request built through it — chat, extraction, RAG, prompt agents, and workflows — went out with none and whatever the provider defaults to applied, with no way for an admin to change it. A `temperature` field already existed on `UserConfig` and the config API read and wrote it, but nothing consumed it when building a request: a control that looked like it worked and did nothing. **Admin → System Config → Available Models → Advanced settings** now carries a Temperature field per model, alongside the existing per-model `max_tokens` and `timeout` overrides. Leaving it blank sends nothing and keeps today's behaviour exactly, so existing deployments are unchanged. Setting it to `0` pins the model to greedy decoding, which is the case the field exists for — reproducible extraction runs. Values are coerced on the way in: bools and non-numerics rejected, ints accepted as floats, and anything outside `0.0`–`2.0` dropped rather than clamped, because providers reject out-of-range values outright and dropping keeps requests working instead of failing every call until an admin notices. Services that already pass their own explicit temperature (the extraction judge, the KB optimizer) are untouched.
- **A long document can now be answered by a bigger model instead of being trimmed.** A document larger than the chat model's context window was compacted — the middle was dropped — and the model then answered confidently from the part that survived. Measured on a 79-page proposal: 41,213 tokens against a 32,768-token model, with 33 of 79 pages reaching the model, while the same deployment had a 262,144-token model configured that holds it four times over. "The proposal doesn't mention X" can therefore mean "X was in the part you didn't send", and nothing on screen distinguishes the two. Two complementary paths are added, both off by default. **Automatic routing:** an admin nominates a long-document model in System Config, and a request that does not fit is moved to it *before* being sent — nothing is dispatched to a model to discover whether it fits. **Offered choice:** when nothing was nominated, the context-limit dialog now offers a larger model as its first option, ahead of the options that discard content. Both are gated on the stored `privacy` label as a relative ordering (internal is stricter than blank, blank stricter than external; equal-or-stricter is allowed), so a request never escalates to a weaker-privacy model, and the suggestion is computed server-side so the browser cannot route around the gate. A successful route is announced in the chat as an informational notice naming both models and the size, because switching the model without saying so is the same failure as trimming a document without saying so.

### Changed
- **Tuning suggestions is no longer surfaced outside the admin panel.** The optimizer inbox had accumulated an always-on item in the account menu and a badged entry in the activity rail, putting a power-user surface in front of every research administrator — nearly all of whom own nothing the optimizer tunes and for whom the count is permanently zero. Both entry points are removed, along with the shared count hook that fed them. **Admin → Optimizer activity** is now the only linked surface, and its header links through to `/tuning` for the admin's own inbox. The route stays live, so bookmarks and the notification links that point at it still work; the tradeoff, chosen deliberately, is that a non-admin who owns a workflow with a waiting suggestion no longer has a nav path to it and reaches `/tuning` by URL or notification only.

### Fixed
- **A copy of a team workflow now lands in the team library, not the copier's personal one.** Duplicating or importing a team-scoped workflow or extraction set bookmarked the copy into the duplicator's personal library — so the copy filed under "My Library" while the teammates who could actually access it never saw it in the team library at all. `ensure_bookmark` now follows where the object lives: when the creation path carries a `team_id`, the bookmark targets the team library (created on demand if the team has none yet), and only personal-scoped objects land in the personal library. All four server-side bookmark paths get this — create, duplicate, import, and catalog import, for workflows and extraction sets alike. A team that cannot be resolved falls back to the personal library rather than stranding the object, since a bookmark in the wrong place is recoverable and an unbookmarked object is the invisibility defect this function exists to prevent. Existing misplaced bookmarks are left where they are — moving them would yank items out of libraries users have organized. (#673)
- **The admin user-search boxes work again — the route they called never existed.** Two admin surfaces — Verification → Examiners → Search Users, and Admin → Organizations → Add User — called `GET /api/verification/examiners/search`, which was never added to the backend, so every query 404ed. Both call sites swallow errors into an empty result set, so the boxes looked functional and reported "No users found" for every query, including exact emails of real users. Since the search is the only path to a user who is not already an examiner or an org member, granting examiner status to a new user and assigning users to organizations were impossible through the UI at all. The route now exists, admin-gated like its `/examiners` siblings, and delegates to the `verification_service.search_users` function that had been sitting there correct and unreachable the whole time. (#692)
- **Knowledge-base chat can now be steered by an identifier or an exact phrase, instead of ignoring both.** Two near-identical documents that differ only by an identifier — a PI biographical sketch and a Co-PI biographical sketch, a PI current-and-pending and a Co-PI current-and-pending — were not separable by retrieval at all, and naming the identifier in the question did not help. Measured on a 16-document grant packet ingested as a knowledge base (170 chunks): asking where `CSU-PI-001` earned their doctorate ranked the Co-PI's sketch **2nd** and the PI's — the chunk holding the answer — **47th**, outside even the 24-chunk over-fetch pool, so no amount of post-selection could have recovered it. Swapping which role the question named moved the score by at most 0.035 while the gap between the two documents was 0.213; rewriting the identifier *inside* a document moved it by at most 0.043 — both small against that gap. Role identity is simply not a dimension a mean-pooled `all-MiniLM-L6-v2` bi-encoder represents, and the document's title is never part of any embedded vector — so there was no signal to find. The second surface was the same cause with a different shape: asked what the three field sites *are*, retrieval returned the chunk that says there are three and names none, because it is short, topically pure, and echoes the question's own words; the chunk that names all three sat at rank 11, was fetched into the pool, and was thrown away by the top-8 trim. The lexical lane that exists for exactly this — the one that rescues a bare `§ 200.1` — was silent on both, because its trigger required a dotted `part.section` number: `CSU-PI-001` has no dot between digits and `Ph.D.` has no digits, so both turns degraded to a bare vector query. That trigger is now widened. An identifier-shaped token (an upper-case run joined by hyphens and carrying a digit — role identifiers, award numbers, OMB and contract numbers), a phrase in double quotes, and the noun phrase a "what is/are …" question asks about all pin the chunks that literally contain them, through the existing lane and within its existing budget of half the top-k. Both questions above now return their answer chunk in slot 2. Four deliberate constraints keep the lane from costing more than it earns: both a hyphen and a digit are required, so ordinary capitalised words and hyphenated prose ("NSF-funded", "cost-sharing", "Co-PI") never pin; a questioned phrase is truncated at the first function word and must still leave two substantial words after its leading article, possessive or count is stripped, so “what is the name of the vendor?” pins nothing while “what are the three field sites?” pins `field sites`; a *phrase* that merely wraps a term already pinned is dropped, so one concept cannot spend two of the four slots — single tokens are exempt, since one citation is routinely a text prefix of another (“200.1” of “200.10”) and both are lookups the user asked for; and an *inferred* term found in more than 24 chunks is discarded as boilerplate rather than pinned, since a project number stamped in every running header points nowhere in particular — a section number the user cited explicitly stays exempt, because that is a deliberate lookup however often the corpus repeats it. Pinned hits are also no longer taken in ChromaDB's storage order, which is arbitrary: they are ranked by how many times the chunk names the term and how early the first mention falls, so a chunk headed by the identifier outranks one that mentions it once in passing — this reordering applies to the existing `§ 200.1` lane too, where on the one measured case it moved the answering chunk up a slot. Across the 30-question key on that packet: 5 questions improve, 3 change order or membership with answer coverage flat, 22 are byte-identical, and none degrades. This is a retrieval change only — no re-ingest, no re-embed, and knowledge bases built before it behave identically on turns that pin nothing.
- **Workflows deleted from the Library no longer hold their name hostage from somewhere invisible.** A support ticket reported names that could not be reused after the workflow using them had been deleted, with no workflow anywhere in the interface holding that name. The old Library delete removed only the bookmark and left the `Workflow` document behind, and workflows are listed *exclusively* through library bookmarks — so an unbookmarked workflow renders in no listing, no picker, and no search, while `name_conflicts` still counts it and rejects the name. Cascade deletion (`remove_item(delete_underlying=True)`) had already closed that path, but three creation paths were still producing fresh orphans, because the bookmark was left to a second call from the client that only the Library's own create/upload modal ever made: **Make a copy** in the workflow editor, `POST /api/workflows/import`, and the examiner catalog importer. Every copy made from the editor was invisible the moment it was created. Bookmarking now happens server-side where all those paths meet, via `library_service.ensure_bookmark`, so a dropped or failed follow-up request can no longer strand a workflow; the client's own `add_item` call became idempotent rather than being removed, so an older frontend against a newer backend produces one row and not two. Extractions imported through the same code path get the same treatment — the defect was never workflow-specific. A workflow is treated as bookmarked only when some library's item list actually references the row, since a `LibraryItem` no library points at renders nowhere either. `scripts/adopt_orphaned_workflows.py` repairs the ones already stranded: each orphan gets a bookmark in its owner's personal library, so it becomes visible and the owner can rename or delete it themselves. It deliberately deletes nothing — a workflow someone quietly built real work in is worth more than the name it holds, and only its owner can tell which one it is — and skips workflows an automation, project pin, or the verified catalog still points at, since those are reachable through their own surfaces. Run it with `--dry-run` first; it is idempotent, so a partial run is safe to repeat.
- **The workflow list no longer caps at 100 and searches only what it fetched.** `GET /api/workflows` accepted `skip`, `limit` and `search`, but no caller passed them and the query carried no sort — so listings took the first 100 rows in Mongo's insertion order (the *oldest* workflows) and filtered that page in the client. Anything created after the first hundred could not be reached by scrolling or by searching for it by name, which is what left the orphaned workflows above unfindable even where a list existed. Results now come back newest first, `search` is applied server-side across every workflow the caller can see, and the response is an envelope — `{items, total, skip, limit}` — so a caller can tell a complete list from a truncated one instead of silently showing a prefix. The count runs through the same filter builder as the page, so the two can't disagree about what is visible. Search terms are also regex-escaped before being interpolated into `$regex`, which they previously were not: searching for `C++ (v2)` raised an invalid-pattern error and returned nothing. **Note for API clients:** `GET /api/workflows` now returns an object rather than a bare array.
- **The optimizer inbox no longer shows a user tuning runs for catalog items they don't own.** A support ticket reported that any authenticated user opening `/api/optimizer/inbox` received optimizer runs "belonging to every team" — workflow, KB and extraction-set IDs, quality scores, failure detail and extraction field lists — while opening one of those items in the app answered "Workflow not found". The inbox did access-filter every row against its parent item; what it got wrong was which grant counts. It accepted plain *view* access, and view is deliberately fleet-wide for shared definitions: a verified catalog KB is viewable by everyone, so is a global search set, and so is any workflow published to the verified library. Those grants are right for reading a shared definition and wrong for a triage list — they put every catalog item's tuning history in front of every user, none of whom could act on a single row, and the item never appeared in the reporter's own workspace, which is where the "not found" came from. A row the caller cannot manage is now kept only when the item is theirs or their team's, so the view-only row that lets a non-managing teammate see a failure on a team item survives, while catalog-wide visibility no longer qualifies. Examiners keep verified-KB rows: they hold manage rights on those by design and are the ones expected to triage them. No apply path changes — applying was already restricted to users who can manage the item, so nothing was ever settable through this. The endpoint's tests stubbed the authorization layer entirely, which is why a green suite never saw it; they now drive the real predicates.
- **A workflow paused for approval is no longer failed as a timeout.** A run waiting on a reviewer stops reporting progress by design — that is what waiting is — but nothing distinguished it from a run whose worker had died. `tasks.activity.reap_stale_running` swept on elapsed time alone, so any approval left for longer than the stale threshold (30 minutes by default) had its activity marked **failed** with "Timed out — no progress reported for over 30 minutes", and the rail said the same thing client-side. Any review not actioned within the lunch hour was therefore reported as a broken run, while the actual `WorkflowResult` sat correctly at `pending_approval` and resumed fine if the reviewer eventually approved. Rows carrying the new pending-review marker are now exempt from both the server sweep and the client's stale check. The exemption made two adjacent gaps load-bearing, so both are fixed: rejecting a review marked the `WorkflowResult` failed but left the activity at "running" — previously the reaper eventually closed it, and with the exemption in place nothing would have — and an expired review with `timeout_action: "none"` leaves the run paused by design with no one able to decide it, so its activity is now closed explicitly rather than spinning forever.- **Corrected the documented opt-out for the Chromium install.** `backend/Dockerfile` said `WEB_FETCHER_BROWSER_ENABLED=false` would "skip this" alongside the `playwright install --with-deps chromium` line, and the v4.4.0 notes said the same. It does not: that is an application env var and cannot affect a build-time `RUN`. It disables the JS-rendering fallback at runtime while the image still carries the entire Chromium dependency tree — measured at ~1.04 GB and 112 OS packages on top of the base image (87 → 199), including two CRITICAL CVEs with no Debian fix available. Since Vandalizer publishes images that other sites deploy, an opt-out that does not exist is worse than no opt-out documented: a deployer who set that variable reasonably believed they had opted out of the cost. The v4.4.0 entry is left as written rather than amended, since it is a shipped record; this corrects it going forward. Making the install genuinely optional is tracked separately.
- **A JavaScript-rendered page that serves only its shell is no longer ingested as knowledge-base content.** The 2 CFR 200 catalog KB carried a grants.gov policy URL that answers HTTP 200 with a page whose entire body is site chrome — the .gov banner, the nav, a session-timeout dialog — because the real content is rendered client-side. It is not empty and it is not a bot challenge, so the empty-fetch guard and `looks_like_bot_challenge` both passed it and roughly a thousand characters of padlock boilerplate were embedded as a source titled "Lock". Retrieval then served it: the KB answered "How long is the session set to expire?" and "What label appears on the button to continue working?" as though they were questions about the Uniform Guidance, and auto question generation, which reads the sources it is given, wrote 16 test questions about the .gov padlock and the Federal Register access page. Those questions scored 0.35 against a KB average of 0.77, dragging a validation run down by 4.4 points for content that has nothing to do with the regulation. `looks_like_boilerplate_only` now rejects a short page carrying two or more chrome markers, on the same conservative shape as the bot-challenge gate: real pages carry the .gov banner too, and it is the length that separates a shell from a document. The gate runs before embedding, so a rejected page costs nothing and lands in `error` with a message naming the actual cause. `repair_gov_kb_sources` counts such a page as broken, so an already-poisoned source is found by the same sweep that finds cached lockout pages — the refetch path deletes its chunks before re-ingesting, so the junk is purged even though the URL still cannot be read. The dead grants.gov URL is dropped from the `uniform_guidance` seed; existing deployments keep it until it is removed in the UI, because seeding adds sources and never removes them.
- **A source name containing a comma is no longer split into several labels that match nothing.** The test-query importer split the Source column on commas unconditionally, so "Subpart D-iii — Monitoring, Reporting, Remedies & Closeout | §§200.328–200.346" — one source — imported as three labels, none of which name anything. Retrieval precision is scored as the fraction of expected labels found among the retrieved sources, so each invented label both failed to match and enlarged the denominator: a question whose retrieval was perfect scored 0. On the 2 CFR 200 set this put 57 of 150 questions at 0 precision by construction and reported the KB's retrieval precision as 0.54 when the questions with resolvable labels measured 0.849 — a number that reads as a retrieval problem and sends you looking for one that isn't there. Quotes now protect their contents, and a cell containing semicolons treats those as its only separator and leaves commas as content; a cell with neither still splits on commas, so every file that imported correctly before still does. The CSV template gained a row showing a quoted multi-comma source name, and the modal says how to list several sources in one cell.
- **The importer now reports expected-source labels that match no source in the knowledge base.** These import silently and can never be credited, so every question carrying one scores 0 retrieval precision on every run from then on — which is how the 2 CFR 200 set was validated against a source taxonomy ("Subpart E-i — Cost Principles | §§200.400–200.419") that named nothing in the KB it was scoring, with nothing on screen saying so. The import response carries an `unmatched_source_labels` list and the modal shows each unmatched label with the number of questions using it, alongside the existing row errors. The check uses the same substring rule the validation run scores with, so a label reported clean is one that run can actually credit. It runs after the questions are written and is wrapped so that a failure to compute the warning cannot turn a successful import into an error the user reads as data loss.
- **A brief OCR outage is no longer recorded as an unreadable document.** `ocr_extract_text_from_pdf` made three in-process attempts with `2 ** attempt` backoff — about three seconds total — then returned `""`, which callers cannot distinguish from a document that genuinely has no text. Any outage lasting longer than a few seconds (a GPU loading a model, a service restarting during a deploy, a provider rate-limiting a burst) exhausted all three attempts before the service had a realistic chance to recover. The ingestion tasks already declared `autoretry_for=TRANSIENT_EXCEPTIONS`, but none of it engaged: `OcrRequestError` subclasses `RuntimeError`, which that tuple never caught, and attempt-exhaustion returned rather than raising. Failures are now classified by status code — 400/401/403/413/415/422 and a missing input file are permanent and still degrade to PyMuPDF immediately, while everything else raises `OcrUnavailableError` (a `ConnectionError`, so the existing retry machinery catches it). The extraction task's budget goes from three retries backing off from 5s to five backing off 1/2/4/8/10 minutes with jitter, and `Retry-After` is honored on 429/503. Out of retries, the document records that the service was unreachable rather than the generic extraction-failed message — a different problem with a different fix from the user's side. Permanent failures now stop after one attempt instead of three. (#633)
- **A URL that blocks our fetcher now still gets the browser fallback.** `web_fetcher` called `resp.raise_for_status()` before the Playwright fallback, so any 4xx short-circuited the whole function and the browser path could never run — a site that refuses a plain HTTP client but renders normally in a real browser was unreachable by any route. The status error is now deferred until the browser has had its turn, and re-raised unchanged if the browser also comes back empty, so a genuine block still surfaces as a block rather than an empty page. Unchanged when the browser fallback is disabled: the error raises immediately, as before.
- **Sharing an item that can't be copied returns a clear 400 instead of an opaque 500.** `share_to_team` funnelled every unexpected exception into `clone_failed` (500). Once the empty-knowledge-base guard began refusing clones with a `ValueError`, sharing a sourceless KB to a team hit that catch-all — so a condition the user could act on read as a server fault. Refusals now return 400 carrying the guard's own message. (#640)
- **Reworded the blocked-fetch message.** It said the site "blocks automated tools like Vandalizer", implying a fault on our side; on the reported case the refusal came from the site's CDN by network reputation, and a stock browser from the same host was refused identically. It now says the site's CDN refused the connection, and keeps the download-and-upload workaround. (#566)
- **A document whose text could not be extracted is no longer marked complete.** When OCR failed on a scanned PDF, the document was stored with zero extracted text but `task_status: "complete"` — it looked processed, and chat then answered questions about it with "the document does not mention that", which is indistinguishable from the document genuinely not containing the answer. The failure was silent and the wrong answer was confident. Extraction had always recorded the failure correctly; the error was erased afterwards, first by `perform_semantic_ingestion` setting `readying` unconditionally and then by `complete` on both the success and exception paths. Every post-extraction status write now goes through one helper that refuses to advance a document extraction already marked `error`, with the exclusion in the MongoDB query filter rather than a preceding read, because the ingestion stages run on separate Celery queues and can race. A first version of this fix guarded only the `complete` write and the bug survived it — the intermediate `readying` write is the half that matters.
- **Every PDF reported a page count of zero.** `SmartDocument.num_pages` defaults to `0` and nothing ever wrote it: the successful-ingestion update in `document_tasks.perform_extraction_and_update` persisted `raw_text`, `token_count`, and `text_markers` but omitted the page count, so the default was served unchanged by the document, management, and project APIs. The PDF branch now reads the count from the file via `document_readers.pdf_page_count` (PyMuPDF's `page_count`, made public since it's now used across modules) and persists it. The count comes from the PDF rather than from `len(page markers)` because `_pymupdf_extract_with_pages` emits no marker for a page with no text layer and no form-field values — counting markers would silently undercount scanned and mixed PDFs, exactly the documents where a page count matters most, while looking correct on clean text PDFs. Only PDFs are counted: XLSX markers describe sheets, not pages, and other formats produce no markers at all, so neither gets a fabricated page count. An extraction that yields no text now also clears `num_pages` alongside `raw_text`, so reprocessing a document that has started failing can't leave a stale count next to empty text. Existing documents keep their `0` until reprocessed.
- **Document chat is hardened against answering with a neighbouring-but-wrong value when the asked-for fact isn't in the document.** Benchmarking document-scoped chat against real grant proposals showed the dominant failure was not invention from nothing but substitution: asked for an indirect cost rate when only a fringe rate exists, models returned the fringe rate with a genuine citation to a real line — the citation checks out, so the wrong answer survives review. `DOCUMENT_CHAT_SYSTEM_PROMPT` now carries the same class of absence-handling guidance `KB_CHAT_SYSTEM_PROMPT` gained in the earlier grounding fixes: check whether the document actually states the specific thing asked; never substitute a related-but-different value; a blank form field is blank, not zero; don't supply standard boilerplate (cognizant agencies, rate-agreement dates) the document doesn't state; and a stated "None" is an answer, not an absence. On a 16-item machine-verified absence benchmark, this wording took three local models from 9/16, 10/16 and 8/16 correct to 16/16, 15/16 and 14/16, with positive controls (facts genuinely present, including a literal "None" field) unchanged — the guidance does not cause over-declining. (#610)
- **Estimated page numbers were shown as exact ones in KB chat, workflow citations and extraction sources.** Scanned PDFs have no page structure to read — their text comes from the OCR endpoint, so `_interpolate_page_markers` estimates each boundary by spreading the known page count evenly across the text. Those markers were the same shape as the measured ones from `_pymupdf_extract_with_pages`, so every downstream consumer treated a guess as a measurement: KB chat rendered `(p. 12)`, workflow citations rendered `· p. 12`, and extraction source pins rendered `p. 12`, none of them distinguishable from a page boundary read directly out of the file. On a scanned proposal the cited page can be off by one or more, which is exactly the kind of confident-but-unsupported answer a reviewer cannot check without opening the document. Chunk metadata now carries `page_approximate` alongside `page`, resolved through a single shared `page_locator` helper so all four consumers hedge identically, and estimated pages render as `p. ~12` in both the model's context and the UI. The flag is written only when true, so chunks and extraction sidecars for measured pages keep exactly the shape they had and existing stored results stay comparable; citations created before the flag existed carry no value for it and render as measured, matching today's behaviour until those documents are reprocessed. (#603)

## [v4.10.0] - 2026-08-10

### Added
- **Per-field source tracking in extraction — click an extracted value to see exactly where it came from.** Clicking a value used to run a blind Ctrl+F for the answer's own wording, which fails for reworded values, common strings, and yes/no answers, and gave no feedback at all on a miss. Extraction now captures a verbatim supporting passage for each field, verifies that passage against the document text, and resolves it to a page — so a value row shows a **p. N** chip and clicking it jumps to and highlights the true source, opening the owning document if it isn't already in the viewer. A field whose passage can't be traced shows an explicit **no source** chip instead of silently doing nothing, which doubles as a hallucination warning. The model is only ever asked for the quote; pages are resolved server-side from the document's stored text markers, and matching is unicode-folded so smart quotes, dashes, non-breaking spaces, and ligatures don't defeat it. Combined-context runs keep per-document spans, so a source attributes to the document it actually came from. Capture is opt-in and enabled only on the interactive run path — optimizer sweeps, the workflow engine, and the management API are deliberately unchanged, so tuned baselines and token costs don't shift under existing deployments. The document viewer also gained normalized highlight matching, a page-level fallback when the exact passage can't be matched, a visible not-found state in the highlight bar, and extraction-highlight support in the DOCX view.
- **Optimizer tuning suggestions are now visible and actionable, instead of accumulating unseen.** Auto-generated optimizer candidates had nowhere to be seen — one production instance had generated roughly 50 suggestions and applied none — and failed tuning runs were invisible entirely. A new **Tuning suggestions** page (reachable from an always-present account-menu entry, plus an activity-rail badge that appears only when something is actually actionable) groups candidates by what they need from you: ready to review, failed, tuning now, applied, no change, dismissed. Each shows the item's name, the signal that triggered the run in plain language, baseline-to-optimized scores, and the apply-preview rollup. Apply reuses the existing preview modal and per-surface apply endpoints, so the tied-with-baseline, cross-field, revert-snapshot, and quality-timeline gates all still apply — there is no second apply path. Dismissing a suggestion soft-hides it while leaving the run in the item's history and in the admin view, so a dismissal is a triage decision rather than a cover-up. Admins additionally get a read-only **Optimizer** tab covering every run across all three surfaces, with surface/status/trigger/window filters and a failure-reason rollup, so a run of silent failures is a one-line answer rather than an invisible one. It is read-only on purpose: applying someone else's config is theirs to do.
- **Notifications now tell you when something fails.** Notifications only ever carried successful events (approvals, verification decisions, catalog updates, team shares); nothing told a user that a workflow run died, a document never finished processing, an extraction errored, or a scheduled automation had been failing every night for weeks — the only way to find out was to go looking. Four failure kinds are added (`workflow_failed`, `extraction_failed`, `document_failed`, `automation_failed`), emitted from the Celery failure paths under three rules: they never raise (a failed bell write must not fail or retry the work that already happened), they fire only on the final retry attempt (so a run about to succeed is never reported as failed), and they coalesce — repeats fold into one unread row with an occurrence count, so a weekly automation failing for a month is one entry rather than thirty, and a 50-file upload with a dozen bad files reads as "12 documents failed to process". This also fixes two bugs that had made the *existing* opt-in failure notifications dead on arrival: the check compared status to `"failed"` while workflow runs record failures as `"error"`, so the per-automation "notify me on failure" email/Teams option had never once fired; and the notification stage was only ever dispatched from the success path, so even with the comparison fixed a failed run never reached it. A passive run that died also used to sit in "running" forever, and is now recorded as failed and linked to its trigger event.
- **Deletions are now audit-logged across resource types.** Deleting a document, folder, knowledge base, credential, extraction, automation, chat, or library item now emits an audit event with actor, resource name, team, and useful detail (a folder's document and subfolder counts, a chat's message count). The admin Audit tab gains filter options and red chips for the new delete actions. Previously an admin reconstructing "who removed this?" had nothing to go on.
- **Bulk import of knowledge-base validation test queries from CSV or Excel.** Evaluators keep complete test sets in spreadsheets, and entering them one at a time doesn't scale. The Test Queries tab gains an **Import CSV/Excel** button (template download, drag-and-drop, per-row result summary) backed by a new import endpoint that accepts flexible header aliases for Question, Expected Answer, Category, Source/Section, Notes, and a stable ID. Rows whose ID matches an existing query update it in place via a new external-ID field, so the same sheet can be re-imported across KB versions without duplicating the set; ID-less rows repeating an existing question are skipped, and row-level problems are reported per row instead of failing the whole import. Test queries also now carry evaluator notes end to end.
- **Docling-Serve is now a supported OCR backend.** Vandalizer only ever spoke one OCR contract — POST the PDF as multipart field `file`, treat the whole response body as the extracted text — so sites running [docling-serve](https://github.com/docling-project/docling-serve) (its prebuilt container image is a common campus OCR deployment) got `POST /v1/convert/file → 422 Unprocessable Entity` on every scanned upload: the file field is named `files` there, conversion options must accompany the request, and the reply is JSON rather than text. Reported by Travis Powell (Montana State University Research Computing). **Admin → System Config → Endpoints** now has an **OCR Service Type** selector: *Plain text response* (the existing behavior, and the default for every existing install) or *Docling-Serve*. Choosing Docling-Serve exposes a **Conversion Options (JSON)** editor that is sent as form fields with each request using Docling's own option names (`do_ocr`, `ocr_engine`, `ocr_lang`, `pdf_backend`, `table_mode`, `include_images`, `images_scale`, `do_picture_description`, `picture_description_api`, …) — booleans, repeated list fields, and nested JSON objects are all encoded the way Docling's form parser expects — plus a **Request Timeout** and a **Use async conversion API** toggle that submits to `/v1/convert/file/async` and polls, for large scans where OCR takes minutes. The endpoint field accepts either the service root or a full convert URL (so `/v1alpha/` deployments keep working), Markdown is read from `document.md_content` with fallbacks to the other output formats, and **Test Connection** now probes Docling's `/health` and reports the exact convert URL uploads will use instead of GETting a POST-only path. Malformed options are rejected when saved rather than surfacing as a 422 on every upload hours later. OCR failure remains a graceful degradation to local PyMuPDF extraction.
- **Garbled text extractions are now detected at ingest and warned about in chat.** A PDF whose embedded fonts defeat text extraction can still yield hundreds of thousands of characters of mojibake — and chat happily accepts questions against it, producing fluent, confidently wrong answers with no signal that the underlying text is garbage (benchmarked: two of three local models invented a complete, specific — and entirely unrelated — topic summary for such a document in 3 of 3 runs; prompt-level mitigation reduced but did not eliminate it, so the safety belongs in the app). Extraction now computes the fraction of non-letter characters in the extracted text (clean documents measure ≤0.01, a mangled text layer ~0.5; threshold `extraction_max_nonletter_ratio`, default 0.25) and stores it on the document. Documents over the threshold get an amber warning icon in the file list, and document chat streams a `context_notice` naming the affected documents and suggesting Retry extraction / re-upload — answers still flow, but the user is no longer flying blind. Documents ingested before this change are unmeasured and never flagged; re-running extraction measures them. (#609)
- **KB validation results can now be exported (CSV / Excel / JSON).** Evaluation results previously had to be recorded manually to analyze, document, or compare runs outside Vandalizer. Every full validation run in a KB's quality History tab (and the run just completed in the Run-now tab) now has an export control producing one row per test query: question, category, expected answer, with-KB and no-KB answers, judge score/verdict/confidence/reasoning, missing and hallucinated facts, retrieved and expected sources, retrieval precision, lift, and discrimination — plus run-level metadata (run id and date, judge model, mode, overall score, catalog version, KB seed id) repeated on every CSV row so exports from several runs concatenate into one comparison table. Excel exports get a Summary sheet with the run metadata alongside the Results sheet; JSON carries the same data natively typed (`vandalizer.kb-validation-results.v1`) for automation, and `run_uuid=latest` is accepted for scripted pulls of the newest run. Export uses the same view gate as the quality history, so view-only evaluators can export without manage rights. Optimizer-apply timeline rows record a config change rather than a measurement, so they carry no per-query results and don't offer the export.
- **A "What's Working" surface for positive feedback.** Positive signal was write-only: chat thumbs-up and high extraction star ratings landed in the database with no reader, and only thumbs-*down* drove any downstream action. A new support-gated **What's Working** tab in the Support Center unifies three sources — chat thumbs-up with a comment, extraction ratings of 4 stars and up, and a new off-ticket product-feedback channel. Chat capture is now symmetric (thumbs-up opens the comment box too, and submits with the actual rating rather than a hardcoded "down"), and a "Something working well?" affordance in the support panel posts praise without ever creating a ticket. Ticket-form copy is reframed off bug-only.
- **An interactive LLM cost estimator in the in-app docs.** An admin can now size LLM API spend per research administrator per month before committing to a model: pick a workload level, a model, and a user count, and the estimate recomputes as you type. Per-operation token draws are derived from how the app actually runs rather than guessed — extraction defaults to the two-pass strategy (so the document body goes through the model twice, which is why it dominates the bill), and document ingestion costs nothing in API terms because chunking and embedding run through a local embedding function, so an upload only bills the classification/title pass. Operation IDs match the metering labels written to the usage ledger, so the table lines up 1:1 with real usage data, and a "Calibrating against real usage" subsection carries the aggregation that replaces the assumptions with measured per-feature averages. The monthly volumes behind Light/Typical/Heavy are stated plainly as plausible estimates for a sponsored-programs office rather than measurements, every field is editable, and model rates (captured 2026-07-31) are editable overrides with dated caveats on the figures that come from sources moving faster than this file does. A "Sizing your own cluster" section picks up the thread the assumptions list leaves hanging — that self-hosted models cost nothing per token but shift the spend to GPU infrastructure — and hands off to MindRouter's configurator, marked as an external tool not affiliated with Vandalizer and scoped honestly to hardware rather than staff time, cooling, or rack space.
- **A kill switch for scheduled promotional email.** `PROMOTIONAL_EMAILS_ENABLED=false` in `backend/.env` silences all three scheduled campaigns — demo recapture drips, onboarding drips, and inactivity nudges — each returning early before it even queries for recipients. Transactional mail (password reset, invites, support, approvals, trial status) is unaffected. This also fixes the recapture campaign's missing opt-out: it was the only one of the three that ignored email preferences entirely, so demo signups had no way to stop the three-step chase sequence. It now honors the same preference key the inactivity nudge uses, and opting out retires the sequence rather than deferring it, so the application stops coming back as due on every daily run.
- **Support tickets: delete a message, and pick tags from a dropdown.** Ticket authors can delete their own messages and admins can delete any, behind a destructive confirm — regular support agents deliberately get no such power, which is tighter than attachment deletes. Deleting a message also removes its linked attachments and their on-disk blobs. Separately, **+ Add tag** now opens a dropdown of existing tags that filters as you type, offering **Create new tag** only when the text matches nothing that already exists — which prevents typo-duplicates like "Deploy" vs "Deployed" — and blurring the field now closes it without committing instead of silently creating a tag.
- **The workflow picker sorts pinned and favorited items first.** The Select Workflow dialog's Mine and Team tabs list pinned items first, then favorited, then the rest, with the same pin/star icons the Library uses. Applies to every picker kind (workflows, extractions, prompts, formatters), which share one component. The Explore tab is unchanged, since catalog items carry no per-user pins.

### Changed
- **PDF ingestion skips the OCR round-trip entirely for digitally-native PDFs.** Every uploaded PDF was previously sent to the configured OCR endpoint first, even when it was already text-based — a research-admin proposal or budget rarely needs OCR, but paid its network round-trip on every upload regardless. A new local classification pass ([pdf-inspector](https://github.com/firecrawl/pdf-inspector), a Rust PDF classifier, ~1-40ms per file) now runs first: a confidently text-based PDF is extracted straight to structured Markdown locally, with real table and heading detection that the existing flat-text fallback doesn't have; anything scanned, image-based, low-confidence, or erroring falls through to the existing OCR-first flow unchanged. Page markers are preserved for citations. No config or admin-facing change — OCR remains the path for scanned/mixed documents.
- **Admin panel loads faster and its tab visibility rules live in one place.** The 16 admin tab components (usage, users, teams, config, etc. — each its own file since the earlier decomposition) are now lazy-loaded behind a single `Suspense` boundary instead of all being bundled into the initial admin chunk. Which tabs a role can see was previously computed by three separate, slightly different filters (one per role tier); it's now a single `minRole` predicate per tab, so a tab's visibility can't drift out of sync across roles. A `?tab=` deep link (e.g. from the catalog-update notification) is now validated against the tabs actually visible to the current user before switching, instead of trusting the query string. Added the first test tranche for the extracted admin components (shared primitives, Audit, Certifications, Email Analytics tabs).
- **`Admin.tsx` is broken up into one file per tab, and `ConfigTab.tsx` (2,770 lines) starts being broken into panels.** All 16 admin tabs moved out of the single `Admin.tsx` monolith into their own files, along with the shared primitives (status/role badges, KPI cards, sortable headers, avatars, search/export controls) and the date/CSV formatters — the latter consolidating 22 byte-identical copies scattered across 8 tab files into one shared module, which incidentally retires three drifted variants and fixes a negative-UTC-offset bug that rendered "Invalid Date" on API-key expiry and compliance retention dates. Then within Config, the UI Theme, Authentication, and Available Models sections became their own components, characterized by a new test suite *first* so each extraction could be verified against a snapshot of the existing behavior; the panel that can lock every user out of the instance keeps its two guards (the auth-methods floor and the `***` client-secret sentinel) now covered by tests in isolation. This is the first phase of a larger decomposition — the remaining Config sections stay in `ConfigTab.tsx` for now.
- **Download and Save-to-folder now offer the same six export formats.** Download had Word but no Markdown; Save to folder had Markdown but no Word — so getting a Word file into a Vandalizer folder meant downloading it and re-uploading it by hand. Both surfaces now offer JSON, CSV, PDF, Word, Markdown, and plain text, sharing one Word renderer (extracted into a new service so saved and downloaded documents carry the same branding) and one Markdown renderer (whose table headers are now collected from all rows rather than just the first). Both endpoints gate on a single valid-format set: the download endpoint returns **400** on an unknown format instead of silently falling back to JSON, and saving to a folder raises a clear error instead of writing a stringified object to a mystery file.
- **The standalone `/workflows` page is retired.** The workspace Library tab is the canonical workflows surface; the `/workflows` list page was unreachable from navigation, and its delete / remove-from-team actions now live in the Library delete flow. `/workflows` redirects to the workspace, matching the pattern `/chat` and `/library` already follow. `/workflows/{id}` share-link deep links still work.
- **Error toasts stay until you dismiss them.** Errors auto-dismissed after 4 seconds, which is not long enough to read a blocked-website message (URL plus reason), let alone select and copy it into a support ticket — and clicking the toast to hold it dismissed it instead. Error toasts now persist until closed via a visible X, don't dismiss on body click, and allow text selection. Success and info toasts keep the existing 4-second auto-dismiss. Since errors no longer expire on their own, two 15-second background pollers that toasted on every failed refresh were silenced, and the stack is capped at five so a repeatedly-failing background job can't pile toasts past the bottom of the viewport.
- **The frontend CI gates now actually gate.** Three quality gates were quietly vacuous. `tsc --noEmit` is a no-op against this project's solution-style tsconfig — it checked **zero** files — and is now `tsc -b`, which builds both referenced configs and checks 1,440 (verified by introducing a deliberate type error and confirming it fails). Coverage was measured against an import-driven denominator, so the reported number moved with which files a test happened to import and could *fall* when a large untested file was pulled into its first test; the denominator is now the whole `src/` tree, making the number honest and monotonic, with thresholds single-sourced in `vitest.config.ts` and set just below the real measurement (~6% lines) rather than the mismeasured one that was failing CI on every push. `make release-check` is unaffected in shape — these changes only make it mean something.

### Fixed
- **A timed-out Code step permanently cost a Celery worker a CPU core.** Sandboxed user code ran in a daemon thread and, on timeout, returned "timed out" while leaving the thread running — Python cannot kill a thread from outside, so `while True: pass` kept spinning for the life of the process, holding the interpreter lock between switch intervals. In a Celery worker that is permanent: prefork children are long-lived, and the task-level time limit doesn't help because the task itself completes normally; only the abandoned thread keeps running. A watchdog now delivers an exception into the runaway thread asynchronously, which CPython acts on at the next bytecode boundary, retrying a few times in case sandboxed code wraps its body in a bare `except` that swallows the first delivery, and warning if it still can't be stopped. Measured on the reported case: thread count returns to baseline, idle CPU goes from 2.02s per 2s to 0.00s, and lock-sensitive I/O elsewhere in the process goes from 4,027× slower back to ~1.1×. A runaway inside a single C call still cannot be interrupted. (An earlier trace-based attempt worked only on Python 3.12 and cost ~5.4× on ordinary sandboxed code; the current approach costs nothing until a timeout actually happens and behaves identically on 3.11 and 3.12.) Two test-only network stalls were fixed alongside it — the unit suite was blocking ~19 seconds per dispatch retrying a Redis result backend that CI doesn't run, and the whole session was running under a live error-reporting transport aimed at the internet — taking `make backend-test` from **23m45s to ~24s** with no tests skipped. (#615, #623)
- **The nightly inactivity-nudge job crashed on a naive/aware datetime comparison.** The Mongo driver isn't configured timezone-aware, so a stored-as-UTC login timestamp reads back *naive*; subtracting it from an aware "now" raised a `TypeError` that took down the entire nightly engagement run. It surfaced only recently because the crash sits behind a "are there any newly-verified catalog items?" guard, and three newly-verified workflows were the first thing to get an inactive user past it. Both read-back sites are now normalized through a local helper matching the idiom already used elsewhere in the codebase — the reported crash on days-inactive, and the same bug latent in the nudge-cutoff comparison, which had avoided firing only because that field stays null until a nudge actually sends (so fixing the first alone would have crashed on the *following* night). Database-side filters were never affected, since those compare dates server-side.
- **`ENVIRONMENT=production` on an HTTP-only install silently broke login.** The `Secure` attribute on the auth and CSRF cookies was decided by the environment label alone, so an air-gapped or intranet "Vandalizer in a box" served over plain HTTP had its cookies silently dropped by the browser: login returned 200 and every subsequent request 401'd. The CSRF cookie was doubly broken, since production mode also selects the `__Host-` name, which browsers refuse to store without HTTPS. `Secure` is now derived as *production **and** the frontend URL is not plain `http://`*, with an explicit `COOKIE_SECURE=true|false` override that always wins; the CSRF layer also accepts a stale `__Host-` value left in the jar from an earlier HTTPS visit. A startup warning fires when production is paired with an `http://` URL, `setup.sh` and `deploy.sh` warn at deploy time, and both `backend/.env.example` and [DEPLOY.md](DEPLOY.md) document it.
- **Workflows with more than one approval gate only ever paused at the first.** Two compounding defects: the resume path never checked the engine's approval-pause sentinel, so a second gate's sentinel was written to the run as if it were the final output and no review was ever created; and the pause handler identified the paused step by scanning for the first node *named* "Approval", which every approval node is — so fixing the first defect alone would have recorded the first gate's position on the second pause, and approving it would have replayed the middle steps into the same gate forever. The engine now stamps the index of the node that actually paused, which is the only unambiguous identifier, with the name scan kept as a floored fallback for sentinels already in flight. Resuming a run also no longer re-pauses incorrectly at a gate it already cleared.
- **Any LLM step after an approval gate failed with a 401 the moment the review was approved.** The resume path resolved the run's model from a config key nothing ever writes, so every resumed step fell through to a hardcoded `gpt-4o-mini` literal — a name absent from System Config, for which no API key exists, so the literal string `no-api-key` was sent to the provider. The run's real model (user preference, else system default) is now snapshotted on the run record at dispatch and read back on resume, with runs predating the field falling back to the configured default rather than a hardcoded name. The resume path also now passes the optimizer's per-step model and prompt-variant overrides, which were previously dropped silently after every approval. This only ever bit runs with an LLM step *after* a gate, which is why Extraction → Approval → Approval resumed fine while Extraction → Approval → Prompt → Approval died.
- **A failed workflow step posed as a successful result.** A step failure (blocked URL, HTTP error, bad config, sandbox rejection) returned its error text as the step's normal output: the engine passed that error string downstream as source material, marked the run **Completed**, and offered Download and Save-to-folder beside it. Failing nodes now attach an explicit error to their result and the engine raises after persisting the step's diagnostics, so the run is marked failed with a "*Step* step failed: *reason*" message that the existing error panel renders — no retry, no crash-report noise, and downstream steps never receive error text as content. Because these failures are deterministic, passive runs no longer schedule futile retries that only minted more failed results, and a passive run that died no longer sits in "running" forever. The step-level **Test** button surfaces the same failure cleanly, keeping its request-preview diagnostics, rather than degrading to a mangled generic exception when read back through Celery's result backend.
- **Concurrent ingest runs could add the same KB source twice.** Two concurrent add-URL runs — a double-submitted Add URLs modal is enough — could both pass the check-then-insert dedup and ingest the same page twice; the reported case showed one crawled document stored as two identical 378-chunk sources. A unique index on (knowledge base, URL) is now built at startup, and on encountering pre-existing duplicates it self-heals: it deletes the newer copies and their chunks, keeps the oldest, recalculates stats, and retries — so existing knowledge bases are cleaned up on deploy rather than needing a script. Every URL-source insert path (seed, crawl child, clone, both import paths) now skips instead of double-ingesting when it loses the race, stats are recalculated unconditionally so an all-duplicate run no longer strands the KB in "building", and the modal's submit button latches so a double-click fires one request.
- **Long web pages were silently truncated on ingest, poisoning retrieval.** Raw HTML was sliced at the *extracted-text* limit (500k) **before** text extraction ran. Markup dwarfs readable text, so a long `.gov`/`.edu` page blew past that limit well before its body ended and the extractor only ever saw the head of the document — while the source showed "ready" with a green check, so a regulation page cut off mid-document looked perfectly healthy and gave wrong or incomplete answers about everything past the cut. Raw HTML now has its own much larger cap and the text limit applies only to extracted text, so real documents reach the extractor whole; a page that genuinely does hit either limit carries a truncation flag through ingestion and shows an amber warning in the source list plus a banner in the inspector, instead of a clean check. Separately, a crawl seeded from one link used to spend its page budget on site chrome (Home, Topics, A–Z index) that fetches cleanly and lands beside real documents as near-contentless sources; crawled pages now pass a content gate before embedding — navigation pages are still followed for their links but never retained, so the page budget counts documents you actually wanted, and a rejected page costs no embeddings. A URL you pasted yourself is never gated.
- **KB validation flagged correct answers as hallucinations, because the *questions* were wrong.** A reported case showed the judge flagging "Hallucinated: Year 2024 as acceptance year" when the answer quoted "Accepted: January 14, 2024" verbatim from the source PDF. The defect was in the auto-generated test query, not the model: the generator authored an "the excerpt does not specify" expected answer from a 600-character chunk sample that the full document flatly contradicts. The generator now requires self-contained questions and drops any scoped to its own private sample, and verifies absence-claim expected answers against full-KB retrieval with a checker pass, dropping contradicted questions (failing open on infrastructure errors). On the judge side, generator categories now map to the matching rubric — previously every auto-generated category fell back to "factoid" — the judge's context cap is raised from 4k to 12k so the grounding chunk isn't cut off mid-way, and a claim that is grounded in the retrieved context but contradicts the expected answer is now a **warning that the test plan is suspect**, never listed as a hallucinated fact.
- **Deleting a library item left the underlying workflow alive but invisible.** Library-tab delete only removed the bookmark, silently orphaning the backing workflow or extraction: gone from every screen, but still holding its unique name — so recreating a "deleted" workflow failed with a name conflict and there was no trash to empty. Delete now offers **Delete permanently** (which deletes the backing object under the same authorization as its own editor, purges every bookmark pointing at it, and writes an audit event) versus **Remove from library only**, with honest bookmark-only copy when a true delete isn't permitted. Forbidden or unsupported deletes remove nothing.
- **View-only users were shown write controls that failed after they committed to them.** A cluster of permission gates that only ran server-side on submit, so the rejection arrived after the work: opening a verified workflow from Explore loads the *original*, and the task editor ignored the manage flag — view-only users got an editable prompt and a live Update button whose save failed silently (unawaited, uncaught, and answered with a misleading 404). The knowledge-base detail pane offered Add Documents, Add URLs, title and description edits, Share with Team, and per-source rename/remove to non-managers, each ending in a 403 or a misleading 404. And "Suggest from history" loaded candidates for view-only users but failed on save with "Workflow not found". Now the KB response carries a `can_manage` flag computed by the same predicate the write endpoints gate on — so the button state and the 403 cannot disagree — and every gate in that pane reads it instead of a client-side guess at the rule, which had wrongly denied team managers their own team's KBs. The task editor shows a "Save a copy to my library" notice instead of an Update button, task-save failures surface as an error toast, and the backend returns **403 with an actionable message** rather than a bare 404 for the no-permission case on task mutations and on saving test cases. Disabled controls state their reason visibly, since a disabled button's tooltip is unreachable by keyboard and touch. Submit-for-Verification is deliberately left enabled — anyone who can see a KB may nominate it for the catalog.
- **Knowledge bases were hard to adopt, clone, and reason about from Explore.** Five related defects. The **Clone** action had vanished from KB cards entirely: a May fix put it on the old card component, a parallel refactor replaced that component, and the merge silently dropped the action — leaving a working clone endpoint with no UI. It's back on every card in My KBs and Team (reference cards clone the canonical source; broken bookmarks offer nothing), with regression tests pinning it to the card so the next refactor can't lose it unnoticed. Adopting a KB from the global Explore tab didn't refresh the My KBs list, so the new KB only appeared after a full page reload. Adopt handlers swallowed *every* error as "Already in your knowledge bases" — a genuine re-adopt succeeds and returns the existing bookmark, so any error there is a real failure and now surfaces as one. References whose source KB no longer resolves (deleted, un-verified, retired from the catalog, scoped away) were silently dropped from My KBs and now render as an "unavailable" stub with a Remove action. And Duplicate / Send-to-team now work for knowledge-base library items, which the clone helper didn't handle. Separately, a verified catalog KB with **zero indexed chunks** showed "Open in Chat" enabled in the Explore preview, where chatting produced a misleading "still indexing" reply; chat is now disabled everywhere a KB has no chunks, with the reason stated visibly.
- **Explore collection counts didn't match what the page rendered.** Collection badges in the KB Explore tab used a kind-agnostic count, so a collection showing "15" could render a single knowledge base — the other 14 items being workflows that only appear in Library Explore. Both collection endpoints now accept a kind filter that scopes counts to one item kind and drops collections with nothing of that kind. The "All Knowledge Bases" / "All Items" badges were also bound to the live *filtered* total, so selecting a collection silently changed them; both tabs now keep and render a separate unfiltered total.
- **The Support Center ticket list silently truncated past 200 tickets.** It fetched a single page of 200 with no total and no way to fetch more, so any filter matching more than that quietly cut off while the stats cards showed the real counts. The list endpoint now returns a total alongside the page, and the Support Center shows "Showing X of Y tickets" with a **Load more** button that appends the next page, deduping by ID since the updated-at sort can shift rows between pages. Ticket rows were also `<button>` elements, and browsers block text selection inside buttons — so a subject or description preview could not be highlighted or copied into another ticket or an email. Rows are now selectable, keeping Enter/Space keyboard activation, and a click that merely finished a text selection no longer navigates.
- **An SSO-linked account could change its email, and the change was silently undone.** The provider-managed guard only checked for a missing password hash, so an SSO-backed account that had *also* set a local password could edit its email in Account: the change passed re-authentication, wrote an audit entry, and emailed the old address — and then the next SSO sign-in silently synced the email back from the identity provider with nothing in the ledger to explain it. The provider is now stamped on the user record at SSO creation and on every SSO login (backfilling existing accounts on next sign-in), profile email changes are rejected for provider-linked accounts, the Account email field is disabled with an explanatory note, and a login that overwrites a diverged email now writes its own audit event so the ledger explains the stale entries.
- **`Failed to fetch` leaked into the UI on any network blip.** The browser's raw error for a request that dies before any response — a backend restart, a Wi-Fi or VPN blip — surfaced verbatim to users. The API client now translates network-level failures into "Couldn't reach the server — check your connection and try again", retries **GET**s once after 500ms on a transient failure (mutations are never auto-retried), and no longer leaks the raw error when the network dies mid-token-refresh.
- **Failed uploads vanished before you could read why.** The auto-clear timer removed successful and failed uploads alike, so an error message disappeared on a timer; failures now persist with their message until explicitly dismissed via a new close button, and survive a subsequent upload batch. Dropped files with unsupported extensions were also sent to the server anyway — drag-and-drop bypasses the file picker's accept filter — and are now marked failed immediately with a message listing the supported formats.
- **Clicking a different run of an already-open extraction or workflow did nothing.** Activity-rail clicks stash the run's saved values in a ref, but the panels consumed it only in an effect keyed on the open item's ID — so re-clicking within the same item never re-ran the load. Open-signal counters now bump on every open, so the values reload even when the ID is unchanged.
- **The knowledge-base optimizer could apply a config the data can't distinguish from the current one.** The KB Validate & improve tab applied the winning config directly, while the workflow and extraction panels confirm through a preview modal and block statistically-tied runs — so the KB flow is now wired to the same behavior: Apply opens the preview (per-query change/regression table, acknowledgement checkbox on significant regressions), and tied runs disable Apply behind an amber "No significant improvement" banner with the confidence interval. The tie guard was also **frontend-only** on all three surfaces — the management API, a stale tab, or any non-UI caller could write a tied config regardless — so the KB, workflow, and extraction apply endpoints now return **409 `tied_with_baseline`** unless the caller sends `force=true`, the escape hatch for a deliberate apply such as a tied score on a cheaper model, following the convention the extraction endpoint already used for cross-field conflicts.
- **Optimization results were described in terms that meant nothing to the reader.** "No baseline / Your settings / Optimized" told a user who never configured anything nothing at all, and "No significant change" read as flatly contradicting bars showing 50% / 89% / 100%. Bar labels are now per-domain and self-describing ("Model alone (no KB)", "Current KB settings", "Optimized KB settings", with extraction and workflow equivalents). The amber banner asserted "treat as equivalent to default" even below the card's own reliability floor — the reported run had three queries against a floor of five — and now says "Not enough queries for a significance call" and asks for more test queries instead of claiming equivalence. The significance test covers only per-query answer-quality scores while the bars show a blended score, which is how the bars can differ by 11 points while the test reports zero lift; the banner and confidence-interval explainer now disclose that gap whenever the two diverge by more than 2 points, and the improved/regressed counter is labeled as computed on training queries while the interval uses held-out ones. Separately, the score-formula **i** icon used the native `title` attribute, which needs a long motionless hover and never fires on touch — users saw only a help cursor and reported it broken — and is now a real popover (hover, click, focus, Escape, click-away) shared by the KB, workflow, and extraction panels. Validation cost figures are now labeled as estimated token usage rather than reading like a charge.
- **Certification Module 10 was unpassable without another person.** The governance module only passed once an admin or examiner set a workflow's verified flag, which nothing but queue approval does — so every certifying user was blocked on someone else, and submitting for verification notified nobody, so requests sat unreviewed. The module content compounded it by instructing users to "mark the workflow as verified in the workflow settings", which is not a thing that exists. The module now passes on at least one workflow submitted for verification at any status, with approval still earning the second and third stars so the credential keeps its teeth; every admin and examiner now gets an in-app notification and an email when a request is created (skipping the submitter, best-effort so a mail failure can't fail the submission); and the module content is corrected.
- **Brand-colored buttons could fail WCAG AA contrast.** Several buttons hardcoded black or white text on top of the configurable highlight color. That's correct for the default gold (black text, 10.95:1), but an admin-configured brand color can invert the requirement — a deployed dark green measured **1.75:1** with hardcoded black text, well under the 4.5:1 requirement, where white gives 12.01:1. These sites now use the computed-contrast variable the branding layer already derives and 37 other call sites already used, keeping the previous hardcoded value as the CSS fallback so unbranded deployments render identically. Separately, the admin Teams tab's Create-team and Add-to-Team buttons sat in a flex row beside a growing input with no shrink guard, so the input squeezed "Create" under its own icon.
- **Member lists showed the email address twice.** Directory display names embed the email in brackets — `Last, First (user@example.edu)` — and the member lists rendered that name plus the email field on its own line. A trailing bracketed email is now stripped from the name, but only when it matches the address shown alongside it, in the Team Settings member table and across the admin Teams tab (detail table, manage rows, isolated users, CSV export).
- **The app render-blocked for 30–90 seconds on networks with no upstream internet.** `index.html` loaded a Google Fonts stylesheet, so on a conference hotspot or an air-gapped install the SPA appeared frozen while the browser timed out the connection. The font is now self-hosted and bundled as local hashed assets — one 26 KB variable file covers every weight the old request asked for, in fewer bytes than a single static family. The built output now contains zero external origins.
- **The admin `?tab=` deep link trapped users on the linked tab.** The deep-link effect re-ran whenever tab visibility recomputed, so after the visibility refactor it re-applied the query-string tab on every render — clicking any other tab snapped straight back, most visibly after following a catalog-update notification. The parameter is now removed from the URL once applied, making the apply one-shot.
- **Every ChromaDB backup was silently empty — including the automatic one `setup.sh --upgrade` takes.** ChromaDB runs as its own service and persists to the `chroma-data` volume at `/chroma/chroma` inside the `chromadb` container, but the backup read `/app/static/db` from the **api** container, which is an ordinary empty directory there and not a volume mount at all. The result was a valid, uncorrupted, **110-byte** tarball containing zero files — which passed the documented `tar tzf` validation and, in `setup.sh`, printed a green checkmark. On this install the real data was **146 MB**. Every operator who followed [OPERATIONS.md](OPERATIONS.md) or upgraded through the supported path had no vector-index backup and no way to know until a restore came up empty — and the restore step pointed at the same wrong path, so it would have silently restored nothing. Fixed in all four places: the backup and restore procedures and the persistent-data map in OPERATIONS.md, the cron example in [DEPLOY.md](DEPLOY.md), and the automatic backup in `setup.sh` (which also needs `--entrypoint sh` on the restore, since that image sets its own). `setup.sh` now additionally counts the archive's entries and reports an empty ChromaDB archive as a **warning** rather than a checkmark, so this class of failure can't pass as green again. Verified end-to-end on a live stack: the corrected command produces a 47 MB / 60-entry archive that unpacks to the full 146 MB, and the new guard fires on the old command. Found during the pre-release install rehearsal.
- **The Celery container reported itself unhealthy on every deployment.** `celery` builds from the backend image, and that image's `HEALTHCHECK` probes the API's HTTP endpoint — but the Celery container runs workers and serves no HTTP, so the probe failed from the first attempt onward and Compose marked the container permanently unhealthy. Every install showed `celery ... (unhealthy)` next to five healthy services, and `./status.sh` closed with "Check celery health: docker compose logs celery" pointing at logs that were completely clean. Worse, it made the signal useless in the other direction: a genuinely dead worker looked exactly like a working one. The service now overrides the inherited probe with one that actually tests Celery — `celery inspect ping` over the broker, which exits 0 when the workers answer and non-zero when they don't (verified both ways, including against a dead broker), with a start period covering worker boot. Found during the pre-release install rehearsal; it applies to the production overlay too, since that inherits this service definition. `./status.sh` on a fresh install goes from 24 passed / 2 warnings to 25 passed / 1 warning, the remaining one being unconfigured SMTP.
- **CI ran an unpinned `npm install -g npm@11` from the registry on every build.** The npm-11 pin was implemented as a global registry install in the CI, end-to-end, and release workflows. Unlike `npm ci`, that install is bound to no lockfile and no integrity hash, and `npm@11` re-resolves to whatever the latest 11.x is at run time — so it executed install-time code fetched fresh from the registry inside runners holding a `GITHUB_TOKEN`, including the release workflow that publishes the container images. That is the exact surface the [Shai-Hulud / keyv-cacheable worm](https://research.jfrog.com/post/shai-hulud-is-back-august/) exploits: install-time lifecycle scripts running on build runners and release pipelines. All three workflows now take npm 11 from the Node 24 toolchain instead, so no unpinned npm install remains anywhere in the build; dependency installation is `npm ci` against the committed lockfile throughout. No lockfile or dependency change is involved. (Worth knowing alongside this: `npm ci` is bound to the lockfile's pinned versions and integrity hashes, but it still *runs* lifecycle scripts — it defends against version substitution, not against a pinned version that is already trojanized.)
- **`cut_release.sh` could stop with no message, or cut the wrong version.** Two failures at exactly the moment someone is cutting a release. The script's `git fetch origin main --tags --quiet` returns non-zero when a local tag has diverged from origin (`would clobber existing tag`), and under `set -euo pipefail` that aborted the whole script with *no output at all* — `--dry-run` simply printed nothing and exited 1, which reads as "nothing happened" rather than "your tags are wrong". This was hit for real during this release: the local `v4.9.0` was a lightweight tag two commits behind origin's annotated `v4.9.0`. Tags are now fetched separately with their error reported, along with the commands to compare and repair. Separately, both the next version *and* the release-notes commit range are derived from the newest **local** tag with no check that it exists on origin, so a stray local-only tag would silently compute the wrong version — verified against the old script, where a leftover `v4.99.0` produced a confident "Next tag: v4.100.0". The script now refuses unless the previous tag resolves on origin.
- **Build and CI hygiene.** Every open pull request failed the frontend and end-to-end jobs in about ten seconds: the lockfile is generated by npm 11, which prunes some top-level optional entries as unreachable, while npm 10 (bundled with the Node 22 images) treats the same file as out of sync and refuses to install. Regenerating with npm 10 isn't stable — npm 11 immediately reverts it — so every `npm ci` call site is now pinned to npm 11, and the affected top-level entries were restored. The frontend Docker build pinned it by running `npm install -g npm@11` on a Node 22 base image, which works on the amd64 CI runners but **fails on arm64** — npm cannot overwrite itself in place on the Alpine image and leaves the global install broken (`Cannot find module 'promise-retry'`), so `make release-check` could not complete on an Apple Silicon machine even though GitHub Actions was green. The builder stage now uses a Node 24 base image, which bundles npm 11 natively, and the fragile self-upgrade is gone. This is a build-stage change only — the runtime image is nginx, so what gets served is unchanged. Separately, two source files in the docs directory differed only in case, so on a case-insensitive filesystem (macOS, Windows) TypeScript resolved the import to the wrong one and the typecheck failed on every developer machine while passing on case-sensitive Linux CI; the module is renamed.
- **Featured Explore collections now describe what they actually contain (catalog v1.3.1).** "Federal Agency Templates" claimed "Extraction templates" but is mostly the NSF/NIH/DOD/DOE knowledge bases (plus the NIHAwardExtract template), and "Pre-Award & Proposal Development" claimed "Workflows" while also bundling the NSF PAPPG and UI APM 45 reference KBs. Both descriptions now name the mix of item types. The seeder now also syncs collection descriptions from the seed catalog on re-seed (previously it only ever created them), so existing installs pick up the corrected text via `./setup.sh --seed` / Admin → Catalog.
- **A failed theme save in System Config failed silently.** Saving the branding/theme panel with a bad response from the server showed no error, leaving the admin unsure whether it saved. It now surfaces the failure. Model/provider edit forms in Config are also now addressed by id rather than list position (same class of bug as the earlier model/provider stable-id fixes).

- **Five admin list endpoints had no upper bound.** Endpoints returning users, teams, workflows, and similar collections to the admin panel had no `limit`, so a large deployment could return an unbounded response. They now request bounded pages and the UI discloses when a list was truncated instead of silently showing a partial result as if it were complete. Also fixed a `RangeError: Maximum call stack size exceeded` risk from spreading a potentially-large array into `Math.max(...arr)`; replaced with `reduce`.
- **Model and OAuth-provider actions in the admin panel could hit the wrong row.** LLM models, OAuth/SAML providers, and their test/probe actions were addressed by their position in the list (an array index) rather than a stable identifier — sorting, filtering, or a concurrent edit could shift positions between when a row was rendered and when its button was clicked, so an action (edit, delete, test, probe) could silently apply to the wrong model or provider. All of these now address by a stable id/tag; test-result badges are also keyed by id instead of list index so a result can't render against the wrong row.
- **A slow admin fetch could clobber a newer one and show stale data.** The audit log and workflows admin tabs re-fetch on every filter/pagination change; if an earlier request was still in flight when a newer one was fired (e.g. rapid filter changes), the earlier response could resolve after the newer one and overwrite it with stale results. Both now debounce state-triggered fetches and cancel/ignore in-flight requests once a newer one supersedes them.
- **Several admin-panel actions failed silently, or let a superadmin lock everyone out.** A cluster of admin mutations and fetches (org/team/user actions, the platform-role toggle, QualityTab's config load) either swallowed errors with no UI feedback or ran without confirmation on a destructive/high-blast-radius change. The platform-role toggle now confirms and shows real errors; remaining silent admin fetches/mutations surface a visible error state; `auth_methods` can no longer be saved down to zero enabled methods (blocked both client- and server-side), which previously could lock every admin out of the app at once; and QualityTab's config load no longer blanks the whole tab for staff users on a transient failure.
- **CSV formula injection in the admin org-import/export flow.** `downloadCSV` wrote user-controlled field values (org names, emails) directly into cells; a value starting with `=`, `+`, `-`, or `@` is interpreted as a formula by Excel/Sheets when the CSV is opened, letting a crafted org name run arbitrary formulas (including exfiltration via `HYPERLINK`/webservice calls) on whoever opens the export. Such values are now prefixed with a leading `'` (safe-quote) before writing. Separately, the org CSV importer is now quote-aware (fields containing commas no longer split incorrectly) and order-independent (parses by header name, not column position), and parsed rows are sorted parent-before-child so child orgs never reference a parent that hasn't been created yet.
- **Chat with nothing attached could present invented document contents as fact.** The system-prompt chain in `chat_stream` covered every grounded context — retrieved KB sources, document/attachment text, a requested-but-empty KB, first-session onboarding, explicit help — and then fell through to `system_prompt = None` for an ordinary chat with nothing attached. `create_chat_agent` turns a falsey prompt into `DEFAULT_CHAT_SYSTEM_PROMPT`, which carries no grounding rule at all, so "What is the total amount requested in this proposal?" could be answered with a specific figure and a line-item budget breakdown when no proposal existed in the conversation; the model disclosed it had no document only when asked for the source page. Reproduced on both an 8B and a 30B local model, so it was the prompt context rather than model capability. The no-context branch now selects a dedicated `NO_DOCUMENT_SYSTEM_PROMPT` that states no document is attached, forbids inventing document-specific figures, quotations, dates, citations or page references, and asks the user to attach the document or select a knowledge base — while still allowing general-knowledge answers, clearly marked as general. This mirrors the guard the adjacent empty-KB branch already had for exactly the same hazard. The selection chain moved into a pure `select_chat_system_prompt()` helper so every branch is unit-testable; behaviour for the KB, document, empty-KB, first-session and help branches is unchanged.
- **Duplicate model tags could silently route a request to a different model than the user selected.** `get_llm_model_by_name` resolves a selector by scanning model names first and tags second, returning the first match either way, and user model preferences are stored and returned as *tags* — so two models sharing a tag made the stored preference ambiguous and let list order decide which model actually answered, with no error and nothing in the UI indicating a substitution. The same first-match precedence meant a model whose **name** equalled another model's **tag** would capture that tag's traffic. Neither the admin form nor `POST`/`PUT /api/admin/config/models` rejected either arrangement. Model names and tags are now validated as one case-insensitive, whitespace-trimmed identifier namespace: the backend rejects a collision with **HTTP 409** naming the conflicting value and the model already holding it (via a new `ensure_model_identity_available` in the existing `name_conflicts` service, matching how workflow/KB/extraction name conflicts are already handled), and the model form warns before submitting. Editing a model no longer collides with itself. Validation runs only on create and update, so existing installs that already contain a collision keep working and can be repaired in place — nothing is renamed automatically.

### Operator Notes
- **Two new `backend/.env` variables, both with safe defaults — no action required unless they apply to you.** `COOKIE_SECURE` forces the `Secure` attribute on the auth/CSRF cookies on or off; leave it unset and it auto-derives (Secure in production, *except* when `FRONTEND_URL` is plain `http://`). **If you run `ENVIRONMENT=production` over plain HTTP on an intranet or air-gapped box, this release fixes login for you with no config change** — previously the cookies were dropped by the browser and every request after login 401'd. Never expose an HTTP-only deployment to the public internet. `PROMOTIONAL_EMAILS_ENABLED` (default `true`) silences the three scheduled marketing campaigns when set to `false`; transactional mail is unaffected. Both are documented in `backend/.env.example` and [DEPLOY.md](DEPLOY.md).
- **Catalog is at v1.3.1 — re-seed to pick up the corrected collection descriptions.** `backend/seeds/VERSION` moves 1.3.0 → **1.3.1**. The seeder now syncs collection descriptions and featured flags from the seed catalog on re-seed (it previously only ever *created* collections, so a description fix could never reach an existing install). Run `./setup.sh --seed` or use **Admin → Catalog** after upgrading. The bundled eCFR content for the v1.3.0 knowledge bases was also regenerated from the source registry, and the two oversized Export Control parts (744 and 774) are now whole-part sources rather than character-split fragments.
- **A unique index on (knowledge base, URL) is built at web-app startup, and self-heals pre-existing duplicates.** On the first boot after upgrading, any knowledge base holding duplicate URL sources has the newer copies and their chunks deleted, the oldest kept, and its stats recalculated, then the index is created. This is automatic and idempotent; expect a slightly longer first startup on instances with large or heavily-duplicated knowledge bases. No script to run.
- **New runtime dependencies.** Backend adds `pdf-inspector` (a Rust PDF classifier shipped as prebuilt wheels — no external service, no config) for the digitally-native PDF fast path. Frontend adds `@fontsource-variable/public-sans` so the app no longer render-blocks on a Google Fonts request, which matters on air-gapped and low-connectivity installs. Both are picked up by a normal `./setup.sh --upgrade`.
- **`npm ci` now requires npm 11 wherever it runs.** The lockfile is generated by npm 11 and npm 10 rejects it as out of sync. The CI workflows and the frontend Docker build pin npm 11 themselves, so Compose-based deploys need no action; if you build the frontend outside Docker, upgrade your local npm.
- **New OCR option — existing installs are unaffected.** `OCR Service Type` in **Admin → System Config → Endpoints** defaults to *Plain text response*, which is exactly the existing behavior. Only select *Docling-Serve* if you actually run docling-serve. See [README.md](README.md#option-1-ocr-endpoint-recommended-for-scanned-documents).
- **Known advisory, assessed and documented — no action needed this release.** `pdfjs-dist` 5.7.284 falls in the range of [CVE-2026-16633](https://github.com/advisories/GHSA-hq66-cqwq-w95j) (arbitrary JavaScript execution from a malicious PDF, CVSS 8.6). The vulnerable path requires embedded-script execution, which lives in pdf.js's annotation layer and bundled viewer; Vandalizer's document viewer uses the raw API and renders canvas and text itself, so the scripting sandbox is never loaded and the path is unreachable — verified against the shipped bundle, which contains no sandbox chunk. The upgrade is a semver-major bump on the PDF renderer and is deliberately deferred rather than taken during a release cut. `DocumentViewer.tsx` now carries a comment recording the invariant and what must happen before an annotation layer or the bundled viewer is introduced. Note that `npm audit` in CI gates on *critical* only, so **high**-severity advisories pass it silently.
- **Check your existing ChromaDB backups — they are probably empty.** The backup procedure and `setup.sh`'s automatic pre-upgrade backup both read the vector index from the wrong container, producing a valid 110-byte archive containing nothing (see the Fixed section). Any `chroma.tgz` taken before this release is almost certainly worthless: verify with `tar tzf chroma.tgz | wc -l` — a healthy archive has tens of entries and is tens of megabytes. Take a fresh one with the corrected command in [OPERATIONS.md](OPERATIONS.md) before upgrading. ChromaDB can be rebuilt by re-ingesting source documents if a backup is lost, but it is slow and requires the originals still be present.
- **Sandboxed Code steps no longer leak a CPU core per timeout.** If you have been restarting Celery workers to recover throughput after runaway Code-node executions, that workaround is no longer needed. No config change.

## [v4.9.0] - 2026-07-23

<!-- The two entries below shipped inside the v4.9.0 tag but were omitted from
     these notes at the time; documented retroactively during the v4.10.0 cut. -->

### Changed
- **`cut_release.sh` emits SemVer `v4.x` tags instead of CalVer.** The release script generated `vYYYY.MM.N` tags that never matched the actual `v4.x.0` release line and would have cut a mis-numbered release if anyone had run it. It now bumps the latest `vX.Y.Z` tag — minor by default (`v4.9.0` → `v4.10.0`), with `--major` and `--patch` — keeps the clean-tree and in-sync guards, dry-run, and PR-subject release notes, and warns when the CHANGELOG has no section for the new tag. `RELEASE_CHECKLIST.md` documents the stamp-CHANGELOG-then-cut flow.

### Fixed
- **The v1.3.0 catalog knowledge bases weren't registered in the regenerable eCFR source registry.** The seed JSONs and manifest gained the Common Rule / FCOI / Export Control URLs but the source registry did not, which failed a manifest test and blocked the release check. All 26 URLs are now registered so the bundled files stay regenerable, and the two oversized Export Control parts (744, 774) moved from character-split fragment URLs — which cannot map back to an API title/part request — to whole-part sources.

- **Two LLM-serving bugs that could make a model never answer or time out unadjustably.** (1) No output cap was ever sent — on a large context window a model could spend the entire remaining budget "reasoning" and never emit a final answer. Every request now carries a `max_tokens` cap equal to the response reserve the budgeter already computes, so output length and input trimming stay consistent; thinking models get headroom (floor 2048) and, on Anthropic, an explicit `budget_tokens` kept below the cap so an answer always has room. (2) The read timeout was hardcoded to 120s for external OpenAI-compatible and OpenRouter models, silently overriding the configured `workflow_llm_timeout_seconds`; the resolved timeout is now sent in model settings (applied per-request across every provider) and the client baselines honor the setting too. Both are threaded through the shared `build_thinking_model_settings()`, so chat, workflows, and extraction inherit them. Two optional per-model overrides expose the levers deployments actually differ on — **Request timeout (seconds)** and **Response reserve (output tokens)** — in the model wizard's Advanced settings; both fall back to the system default / computed value when blank. Also removed the unused `max_context_length` setting.
- Structured extraction against vLLM-protocol models now requests server-side schema enforcement through the standard OpenAI `response_format` parameter (via pydantic-ai's `NativeOutput` mode) instead of vLLM's proprietary `structured_outputs` extra-body field. OpenAI-compatible gateways (LiteLLM, MindRouter, and similar) silently drop unknown extra-body fields, which quietly disabled schema-guided decoding whenever vLLM was reached through a gateway — extractions still ran but without server-side schema enforcement. `response_format` is honored both by vLLM directly and through gateways, so enforcement now applies on either path.
- **SAML SSO can now be configured from the admin UI.** The SAML service, ACS/metadata routes, and the login-page SAML button already existed, but the "Add SSO provider" form and its API schema only collected OAuth fields (client_id/secret/redirect_uri), so selecting **Type → SAML** couldn't enter the IdP metadata (`idp_entity_id`, `idp_sso_url`, `idp_x509_cert`) the SAML service needs — the option was unreachable. The provider form (add + edit) now shows the IdP fields when SAML is selected and surfaces the read-only SP metadata/ACS URLs to register with the IdP; `OAuthProviderRequest` accepts the SAML fields (client_id/secret optional, validated per-provider); and `GET /api/auth/config` now advertises a configured SAML provider so the "Sign in with SSO" button appears. To avoid copying each field by hand, the form can also **auto-fill the IdP fields from the identity provider's SAML metadata** — paste a metadata URL or the XML and a new superadmin `POST /api/admin/config/auth/saml/parse-metadata` extracts the entity ID, SSO URL, and signing certificate. Enables InCommon/Shibboleth-style campus SSO without hand-editing the database.
- **Project/KB chat answers degraded as projects grew — six retrieval-layer fixes, based on the Projects evaluation analysis by Josi Bartholomew (Southern Utah University).** Her 11-file vs 25-file grant-package evaluation showed accuracy dropping roughly 10–18 points with file count and isolated five recurring error patterns; each traced to a concrete retrieval gap, all now fixed. (1) Chat now honors the KB's optimizer-tuned retrieval settings — `k` was hardcoded to 8 and only the similarity floor was read; chat and the validation harness now share one `retrieve_kb_chunks()` pipeline so tuned `k` / rerank / query-rewriting apply identically on both surfaces. (2) One long narrative document can no longer crowd every retrieval slot: chat over-fetches 3× and applies a per-source diversity cap, and when a message names a project file, a source-filtered search guarantees that document a share of the context (fixes short timelines/letters being consistently out-ranked). (3) The model now sees the project's document manifest in its system prompt and distinguishes "that document is in this project but nothing from it was retrieved for this question" from "not in this project" — instead of confidently claiming a fact isn't in the documents. New guardrails also forbid deriving figures that aren't explicitly stated and require same-field/period/unit verification before declaring a cross-document discrepancy. (4) Anaphoric follow-ups ("what about year 2?") are condensed into standalone search queries from recent turns (timeout-bounded, with a non-LLM fallback), so follow-ups keep their grounding. (5) Tables no longer chunk mid-row, and continuation chunks repeat the table's header rows so numbers keep their column labels (page/sheet citation offsets unaffected); a new manage-gated `POST /api/knowledge/{uuid}/reingest` re-chunks an existing KB from stored text so current projects benefit without re-uploading. (6) Every chat answer now persists its retrieved sources as citations — chips survive reload, and future evaluations can distinguish retrieval misses from reasoning errors. KB ingestion also became idempotent (stale chunks cleared before re-add), fixing a latent double-add on Celery retries.
- **Two KB retrieval misses surfaced after the out-of-scope grounding fix — section-number lookups and multi-question turns.** Both traced to the fact that chat ran one embedding-based search per turn. (1) A bare regulation-section citation ("What does § 200.1 say?") carries almost no semantic signal, so vector search returned zero chunks and chat abstained even though the section's text was in the KB; chat now also detects CFR-style section numbers in the message and pulls the matching chunks *lexically* (ChromaDB `where_document` substring filter, with a word-boundary post-filter so "§ 200.1" never drags in "§ 200.10"), pinning them into the final context so a section-only question is answered instead of refused. (2) Several questions in one message shared a single blended embedding and one top-k budget, so a secondary question ("what does 'non-Federal entity' mean?") could retrieve nothing and go unanswered when co-asked — even though it answered perfectly alone; chat now splits a multi-question turn into its individual questions, retrieves for each, and round-robins the pools so every question is represented before the top-k trim. Single-question and anaphoric follow-up turns are unchanged. Grounding behavior is untouched — the model still abstains when nothing relevant is retrieved.
- **Operator tooling to recover KBs and documents broken by moved/blocked source URLs and legacy conversions.** (1) A new `scripts/remap_kb_source_urls.py` re-points KB sources whose URLs are dead (404/DNS) or permanently bot-blocked (403) at their durable eCFR equivalents across every KB (seeded, cloned, user-created), matching by exact URL and marking them for rebuild so a follow-up `repair_gov_kb_sources` run re-embeds them via the eCFR API — the one path that still works from a server blocked from agency HTML sites (covers Common Rule → 45 CFR 46, FCOI → 42 CFR 50 F, Export Control → 15 CFR EAR, Effort Reporting → 2 CFR 200 Subpart E). (2) `migrate_legacy_excel_html.py` gains a `--reingest-render` fallback: when the legacy Flask app kept only the HTML render of an uploaded workbook (the original `.xlsx` is gone), it re-ingests that render in place so the document becomes searchable and opens in the viewer again — recovered as an HTML table, not a native workbook. (3) Dropped three dead supplementary links from catalog seeds (`cfo.gov` from Uniform Guidance, the bot-blocked DOD FAQ, the dead DOE guide) so fresh installs are clean.
- Branded deployments no longer flash the default Vandalizer theme (colors, logo, icon, favicon, title) for ~1s on load before the custom branding appears. The last-known theme is cached in `localStorage` and applied synchronously — by an inline bootstrap script in `index.html` before the bundle loads, and by the `BrandingProvider` initial state before the first React paint — then reconciled against `GET /api/config/theme` on mount. First-ever visit (empty cache) is unchanged.
### Added
- **Three new bundled catalog knowledge bases — Common Rule, FCOI, and Export Control (catalog v1.3.0).** Promotes three regulation KBs into the verified catalog so every deployment — including open-source installs and servers whose outbound access to `.gov` sites is blocked — receives them through the version-aware catalog upgrade instead of per-server scripts: **Human Subjects Protection (Common Rule)** (45 CFR 46), **Financial Conflict of Interest (FCOI)** (42 CFR 50 Subpart F), and **Export Control (EAR)** (15 CFR Parts 730–774, the full subchapter C). Each source ships **bundled `content_file` text** fetched from the official eCFR versioner API, so `seed_catalog`'s create/repair path ingests them offline with no live fetch — the same durable pattern as the existing bundled `.gov` KBs. The two oversized EAR parts (744 end-user/end-use, 774 the Commerce Control List) are split into sub-500k pieces so they stay under the `KnowledgeBaseSource.content` cap and survive a post-seed reingest intact. On upgrade (`setup.sh --upgrade` or Admin → Catalog), `seed_catalog` creates these KBs on existing installs from the bundled text.
- **Native Google Gemini support, plus a guided "Add a model" wizard.** Gemini is now a first-class provider: a new `google` protocol routes through pydantic-ai's native `GoogleModel` (google-genai SDK) using a Google AI Studio API key, so structured output, thinking, and multimodal work through Google's own API rather than the OpenAI-compatibility shim. Vertex AI (service-account auth) is intentionally out of scope for this pass — the path is api_key only. To make this reachable for non-technical admins, the System Config → Models "Add Model" form is now a **provider-first wizard**: step one picks a provider (Google Gemini, OpenAI, Anthropic, OpenRouter, Ollama, vLLM, or Custom); step two shows only the fields that provider needs (usually just a model name and key), auto-filling protocol/endpoint/flags, with every legacy field preserved under an **Advanced settings** expander. On save the wizard runs an automatic connection test and shows a pass/fail diagnostic. Editing an existing model jumps straight to the configure step with its provider inferred. The wizard is also wired into first-run setup: the readiness checklist's "Add a model" blocker launches it directly, and the first model an admin connects on a fresh install auto-becomes the system default — so a new deployment reaches "ready" in one guided pass instead of raising a second "no default model is set" blocker. Two backend hardening fixes ride along: bare `gemini-*` names auto-detect to `google` instead of being mistaken for a local Ollama model, and the vLLM-style `chat_template_kwargs` thinking signal is never sent to native/strict providers (it previously caused a 400 from Google's endpoint).
- **Duplicate a project, and a "What are Projects?" help link that stays accessible.** Each project card in the Projects drawer gains a ⋯ menu with **Duplicate**, which creates a copy (`"… (Copy)"`) owned by you: the new project's folder, implicit knowledge base, record, and pinned resources (workflows, extractions, automations, KBs) appear immediately, while its files and folder subtree are deep-copied and re-ingested into the copy's KB by a background job (`tasks.project.duplicate`) — each copied file gets its own stored bytes, so deleting one project never touches the other's. Memberships and invite links are intentionally not carried over. Separately, the Projects drawer header now has a **"What are Projects?"** pill that reopens the full Projects explainer as a modal (previously it only showed as the empty-state), so the explanation of what a project is and what it contains stays reachable after you've created one.
- **Edit a support ticket's subject after filing.** The ticket title was fixed at creation with no way to correct a typo or vague subject. The ticket header (both the customer support panel and the agent Support Center) now shows a pencil next to the subject that opens an inline editor (Enter to save, Esc to cancel). A ticket's own author — or support staff — can rename it; status, priority, assignment, and tags remain support-only. Backend: `PATCH /api/support/tickets/{uuid}` now accepts `subject` with an owner-or-support authorization check.
- **WCAG 2.1 AA accessibility across the app, plus an automated axe-core regression gate.** The frontend now conforms to WCAG 2.1 Level AA, verified over five review-and-fix passes. Highlights: every form control is programmatically labelled; modals trap focus and restore it on close (via `focus-trap-react`) and close on Escape; a global `:focus-visible` keyboard ring is applied consistently (and controls no longer suppress it with inline `outline:none`); a "Skip to main content" link and `<main>` landmark are present; each route sets a descriptive document title; text and icon contrast meets AA on light surfaces (including an accessible-on-light derivation of the brand highlight color); tab bars, radio-card groups, switches, and comboboxes expose correct ARIA roles/state with `aria-controls`↔`tabpanel` wiring and keyboard operation; status changes (toasts, chat, long-running panels) announce via polite live regions; native `confirm()`/`alert()` were replaced with the accessible in-app confirm/toast; personal-data fields carry `autocomplete`; and animations honor `prefers-reduced-motion`. A new Vitest + `vitest-axe` suite (`src/a11y.test.tsx`) asserts zero axe-core violations on representative remediated components so regressions are caught in CI. Findings for each pass are documented in `wcag_report_findings_pass1..5.md`.
- Branding: a "Hide icon from the navigation header" toggle in System Config → Theme. The custom icon/mascot is still used as the browser-tab favicon and the chat avatar, but can be hidden from the top nav (next to the wordmark) for deployments that only want a favicon. Off by default, so existing branding is unchanged.
### Changed
- Branding logo and icon/mascot uploads now automatically downscale oversized images (on a canvas, client-side) to fit the encoded-size cap, instead of rejecting them with "Image too large." Transparency is preserved (WebP for alpha sources), SVGs pass through untouched, and the stored size stays small. Large exports (e.g. a 1+ MB PNG) now "just work."

## [v4.8.0] - 2026-07-01

### Added
- **Opt-in anonymous deployment telemetry, so the maintainers can see how many self-hosted installs exist and roughly how heavily they're used.** Off by default and privacy-first: when enabled, each deployment sends a once-a-day heartbeat carrying only an anonymous random instance UUID, the version, a coarse environment label, and *bucketed* usage counts (e.g. "11-50 users") — never document content, names, emails, or keys, and every payload is logged locally before it's sent. An optional voluntary identity tier (`TELEMETRY_ORGANIZATION` / `TELEMETRY_CONTACT_EMAIL`) is self-declared only and omitted entirely when blank. Enablement is DB-backed (`SystemConfig.telemetry_config`) so a global admin can opt in or out at runtime — via a new **Telemetry opt-in banner** on the Admin page (shown only while telemetry is off, undecided, and the installer never asked) — without editing `.env` or restarting; the env var is only the initial default. `setup.sh` also asks at install time and now on `--redeploy` / `--upgrade`, reusing the institution name already collected. Separately, a single collector instance can set `TELEMETRY_COLLECTOR_ENABLED=true` to expose the public heartbeat-ingest route and an admin **Telemetry** dashboard (fleet size, version/environment distributions, named deployments); both stay completely hidden on every normal deployment. Full disclosure in [docs/telemetry.md](docs/telemetry.md).
- **"Add Knowledge Base" in the chat composer.** The chat "+ Add" menu gains a third item (alongside Add Document and Add Website) that opens the knowledge-base picker; choosing a KB there activates it for the conversation through the existing flow. No backend change — the chat endpoint already accepted a knowledge base.
- **Staff (analytics/operations role) can reach the tools they need without full admin.** The audit-log query + CSV export, the demo/trial management endpoints, and the System Configuration nav item now allow `is_staff` in addition to `is_admin`, matching how the frontend already gated the Demo tab — so an analytics-role user can do their job without being granted full admin.
- **Prompts and Formatters get their own Library filter chips.** The Library's Mine/Team view previously lumped extractions, prompts, and formatters under one "Tasks" chip, leaving prompts and formatters reachable only via "All". Each now has a dedicated, mutually-exclusive chip with its own count. Frontend-only; no data-model change.

### Changed
- **Warmer, unlimited end-of-trial renewal.** Expired-trial users were dead-ending on a terminal "thank you" page (or a bare landing page) instead of the renewal screen that already existed, and self-extensions were capped at two. The extension cap is removed (renewals are now unlimited; `trial_extensions_used` still increments for analytics only), the GitHub "get in touch" dead-end is gone, and every entry point (login, a mid-session 403, the expiry email) now routes to `/demo/trial-end` with warm copy acknowledging the beta is built with and for trial users.
- **Library terminology standardized on "Extraction(s)".** The Library filter chip read "Tasks" while filtering `set_type='extraction'`, and the create modal/button mixed "Task" and "Extraction". All user-facing Library strings now say "Extraction(s)" and the create button is type-correct (Create Extraction / Prompt / Formatter). Backend `set_type` unchanged.

### Fixed
- **Trial credential recovery was broken in several ways at once.** A cluster of support tickets traced to: the "Resend them" link in trial emails pointing at a route that didn't exist (client-side "Not Found"); activation, per-user resend, and admin bulk-resend each minting a *new* password on every send, silently invalidating earlier emails; activation emails that failed to send being ignored, leaving accounts whose owners never got in; and password reset never clearing a `locked` trial status (which login rejects before checking the password, so reset was futile). Recovery is now magic-link-first — no plaintext passwords emailed, no password rotation on resend, magic-link TTL raised 48h → 14 days — and `resend_credentials` is status-aware (active → fresh link, pending → waitlist message, expired/locked → renewal token routing to `/demo/trial-end`). Email-send failures are now checked and logged.
- **Expired-trial logins dead-ended on a 401 instead of the trial-end screen.** The SPA throws on any non-2xx and never reached its `demo_expired` redirect, so the deployed end-of-trial screen never rendered. `/login` now returns a 200 `demo_expired` payload (no session token) for a locked trial, and the service detects the locked trial before password verification, so the app redirects to `/demo/trial-end`.
- **KB chat silently dropped its grounding guardrails on every follow-up turn.** The grounding prompt was passed as a `system_prompt`, which pydantic-ai only injects on the first request of a run; multi-turn chat rebuilds history from stored message text (no system prompt), so follow-up questions ran with no cite-by-filename / refuse-when-unsupported rules. It's now passed as `instructions`, which pydantic-ai re-applies on every model request.
- **PDF document and URL knowledge-base sources failed to ingest.** A document source read the document's `raw_text` exactly once at "Add Documents" time and errored permanently if extraction (async, OCR-first for PDFs) hadn't finished — so the source never re-ingested once the text filled in. Document sources now park as "pending" while extraction runs and are settled (re-ingested or errored) by the extraction-completion task, preserving page citations. Separately, `fetch_url` decoded every response as HTML, so a direct `.pdf` link yielded nothing; it now sends browser-like headers and, when the response is a PDF (by content-type or `.pdf` extension), extracts via the PDF reader.
- **Out-of-scope KB questions could still get a confident, fabricated answer.** Grounding was prompt-only, so the model could answer from the weakly-related chunks vector search always returns. A deterministic per-KB retrieval-similarity floor (`RAGConfig.min_similarity`) now drops chunks below the threshold before generation; when that empties the candidate set, the existing empty-retrieval path produces a clean "not in the knowledge base" refusal. Ships disabled everywhere (floor `0.0`); turning it on is a deliberate, data-driven step, and live KB chat honors the same per-KB floor as the validation harness.
- **KB "Synthesized Answer" (answer mode) returned a 401 in workflows.** The Knowledge Base Query node was the only LLM-using node not wired to system config in the engine builder, so the synthesis call resolved a `no-api-key` placeholder and routed to the provider's public host. It now receives system config like every other node. ("Passages" mode was unaffected — it makes no LLM call.)
- **KB judge and extraction judge returned a zero-token "Connection error" across Celery event loops.** A cached pydantic-ai Agent carries an httpx pool bound to the loop that built it; reusing a judge agent built during tuning (loop A) on a later "Run Now" validation (loop B) raised "bound to a different event loop" and surfaced as 0% scores / "judge error: Connection error". Both judge caches are now loop-aware (rebuilt when the running loop differs, reused within one run) and are flushed by `clear_agent_caches()` so an admin config save clears them too.
- **A read against a not-yet-ingested knowledge base could create an empty Chroma collection — or fail outright on a read-only store.** Retrieval used `get_or_create_collection`, a write. `query_kb` now uses a read-only lookup that returns `None` when the collection is absent and degrades to no results instead of writing or raising "attempt to write a readonly database".
- **Share-link recipients could open a shared workflow but got a 404 when they ran it.** The run and status-polling paths authorized purely by ownership/team membership and ignored the share token. The token is now threaded through run, batch run, status, batch-status, stream, and cancel (each re-authorizes independently, so patching only `/run` would 404 on the first status poll), with a view-only "Save a copy to my library" affordance. Recipients still run against only their own documents.
- **Examiners got an opaque 404 managing verified knowledge bases.** The frontend enabled manage controls for `is_examiner`, but the backend `can_manage_knowledge_base` honored only owner / team-manager / admin, and the route collapsed "not found" and "not authorized" into one 404. Examiners can now manage *verified* KBs (scoped to verified only), and a new `_require_manageable_kb` distinguishes a genuine 404 from a 403 (logged with context). A "Clone to validate" CTA now gives non-owners an editable copy instead of a disabled button with no path forward.
- **Verified-collection badge counts over-reported.** A collection's `item_ids` is a static curated list that can still point to items since un-verified, deleted, or scoped to an org the viewer isn't in — all dropped when the category opens, so the raw count disagreed with what rendered. Badges now use a `visible_count` computed with the verified + org-visibility filter, and collections with nothing visible to the caller are omitted.
- **The Certification "Batch Processing" module was unpassable for everyone.** Its grader queried `WorkflowResult.user_id`, a field that doesn't exist on the model, raising and surfacing as a generic "cannot be verified, try again later". It now scopes batch results through the user's own `Workflow` ids (the same way every other validator scopes its data). No schema change.

### Operator Notes
- **Five new `TELEMETRY_*` variables in `backend/.env.example`, all safe defaults.** `TELEMETRY_ENABLED` (off), `TELEMETRY_ENDPOINT` (defaults to the maintainers' collector — blank it to hard-disable sending, e.g. for air-gapped installs), `TELEMETRY_PROMPTED` (set true once `setup.sh` has asked, so the in-app banner doesn't re-ask), the optional `TELEMETRY_ORGANIZATION` / `TELEMETRY_CONTACT_EMAIL` identity fields, and `TELEMETRY_COLLECTOR_ENABLED` (turns a deployment into the fleet collector — leave `false` on every normal install). Existing `.env` files predating telemetry inherit the endpoint default so the in-app banner works; nothing is sent until an admin or the installer opts in. See [docs/telemetry.md](docs/telemetry.md).
- **`setup.sh --redeploy` / `--upgrade` now prompt for the telemetry choice** (tty-guarded so it never blocks auto-update/cron, and idempotent — asked at most once, guarded by `TELEMETRY_PROMPTED`), closing the gap where the original install-time prompt only reached new installs. The opt-in is applied on the *same* deploy: `api` + `celery` are recreated once the choice is written, since Compose only re-reads `env_file` on (re)create, not restart.
- **Management `GET /api/mgmt/v1/stats` no longer 500s.** Two independent failures fixed: a `distinct` active-user query was called on a Beanie `FindMany` (no such method) and now routes through the motor collection; and a token-count `$sum` could promote to BSON `Decimal128` (from a single legacy stub document), which `int()` rejected — now coerced defensively. Web-tier Sentry capture was also made deterministic by registering the Starlette/FastAPI integrations explicitly rather than relying on auto-enable ordering.
- **DEPLOY.md gains an LLM provider compatibility table** (OpenAI, Azure OpenAI, Anthropic, OpenRouter, Gemini, Ollama, vLLM, and the generic OpenAI-compatible case), replacing the flat bullet list and fixing the stale protocol list. It explicitly documents that **Microsoft 365 Copilot is not supported** (it's an end-user product, not a model API — use Azure OpenAI for Microsoft-hosted models).
- **No verified-catalog change this release.** `backend/seeds/VERSION` remains **1.1.1**; no re-seed is required when upgrading from v4.7.0.

## [v4.7.0] - 2026-06-22

### Added
- **Projects — a scoped container for one piece of work.** A Project gathers everything for a single grant, contract, or compliance effort in one place: a dedicated folder tree for its files, an implicit knowledge base that auto-ingests every file in the project, the workflows/extractions/automations/knowledge bases you pin to it, and a chat that answers only from the project's documents. Projects are reached from a left-rail **Projects** drawer; the empty state shows an onboarding tutorial (what a project gathers, the four-step setup). Opening a project scopes the whole workspace to it — Files root at the project folder, Chat searches the project KB, and the Automations/Knowledge tabs filter to that project's pins (with a "Show all" escape). A yellow context bar names the active project and offers Manage / Exit. Projects are **personal by default** and never auto-shared; the owner can share with the current team or make it personal again, and can mint revocable **invite links** (viewer or editor, with optional expiry / max uses) for collaborators outside the team. Resources created inside a project (a new KB, a new automation) auto-pin to it, and any catalog item can be pinned from Explore. The active project survives a page reload (the scope is persisted and re-fetched, self-healing if the project was deleted or access revoked), and switching teams clears it. Backed by new `Project` / `ProjectMembership` / `ProjectJoinLink` / `ProjectPin` models and a `/api/projects` router.
- **One unified "Validate & improve" flow across knowledge bases, extractions, and workflows.** The three previously-divergent quality surfaces (KB tuning, extraction validation, workflow validation) now lead with the same **Validate & improve** action and report results on the same scale, with the optimized configuration labeled **Optimized** against your current settings and a no-config baseline. Extraction validation in particular was unified onto the optimizer's closed loop — baseline → judge-scored sweep → apply → post-apply re-validation — and the legacy "Find Best Settings" tuner and divergent score card were removed. A small-test-set discount is applied consistently to both the optimized headline and the post-apply certified score, so applying a better config never *looks* like a regression; the panels disclose the discount and how many test cases clear it.
- **Certified scores measured after apply, plus plain-English trial explainers.** Applying an optimized extraction config now runs a post-apply validation pass on the full test set and writes an authoritative **certified score** to the item's quality tile — no separate validation run needed. Every trial in the results table (KB, extraction, and workflow) is now clickable, opening an explainer modal that spells out, in plain language, the config that trial used, why it was or wasn't selected, and the per-step / per-field outcome breakdown.
- **Cost governance for "Validate & improve" runs.** A per-user concurrency cap (default 3 active runs, counted across KB / extraction / workflow optimizations together; configurable via `SystemConfig.quality_config.max_concurrent_optimizations_per_user`) plus a per-endpoint rate limit (5/min) and an `optimization.start` audit event now guard the optimizer. Exceeding the cap or rate returns a 429 the UI surfaces; super-admins bypass the cap but are still audited. The **Validate & improve** controls are gated on a server-authoritative `can_manage` flag threaded into the extraction, workflow, and KB panels (disabled with a tooltip for view-only users), matching the backend's `manage=True` enforcement.
- **Stale validation-plan detection with one-click regenerate.** A workflow's validation plan can drift out of sync after you edit the workflow's steps. The backend now stamps a canonical definition hash on the plan and flags it stale when the workflow changes (or when a check points at a step that no longer exists). The Validate tab shows an amber banner explaining the drift with two manage-gated actions: **Regenerate plan** (re-derives the auto checks while preserving and re-mapping your manual ones) or **Plan is still correct** (re-stamps the hash without regenerating). Validation responses carry `plan_stale` / `stale_reasons` so the signal is available wherever a plan is read.
- **Choose how a KB evaluation set is built, and edit the questions in it.** The KB Validate & improve wizard's Test-set step now offers three modes — use your saved questions only, generate a fresh set, or combine both — with a coverage level for the generated questions. Generated and saved questions are editable inline in the wizard (and in the standalone Test Queries tab), and you can deselect or permanently remove individual questions before a run; the surviving set is what's actually validated. Saved KB test queries gained a `PATCH …/test-queries/{uuid}` endpoint so per-field edits don't clobber server-owned judge metadata. The same select-and-remove-before-running affordance was added to extraction test cases.
- **Folder actions: Move, Export, Ask, Run, and Add to KB.** The folder context menu in the file browser gained five actions: **Move to…** (reparent a folder, with cycle/ownership guards and the shared team root kept immovable), **Export contents** (download the whole subtree as a structure-preserving `.zip`, reusing per-document authorization), **Ask about folder** (scope chat to just that folder's documents and focus the composer), **Run workflow on folder** (batch-run a chosen workflow over every document in the folder), and **Add to knowledge base** (add the folder's documents to a chosen or newly-created KB). Workflow runs and KB adds now accept `folder_uuids` and expand them server-side to the viewable documents within.
- **Add an entire folder of documents to a knowledge base.** The KB "Add documents" picker now has an **Add entire folder** action with an **Include subfolders** toggle, collecting every non-deleted document in the folder (or subtree) through the normal ingestion pipeline instead of selecting files one at a time.
- **Knowledge Base source provenance and full ingested-text view.** Every KB source now carries an editable **Source** provenance field (defaulting to the origin URL for web sources, free-text for documents), shown in the sources list (URLs linkified) and editable in the source inspector. The inspector also now shows the **complete extracted text** that was actually chunked into the KB — including document sources, which previously showed nothing — so you can see exactly what the KB is answering from.
- **Admin org-wide Knowledge Bases inventory.** A new **Knowledge Bases** tab in the admin panel lists every KB across all users and teams — title, tags, owner, team, source/chunk counts, status, verified flag, and last-updated — searchable and sortable, with inline rename for full admins. Backed by a new `GET /api/admin/knowledge-bases` endpoint that batch-resolves owner emails and team names.
- **Reuse saved Prompts and Formatters in workflow steps.** Prompt and Formatter steps can now link a saved Library prompt/formatter (mirroring "Use Saved Extraction Set"), so a single edit to the saved item propagates to every workflow that references it at runtime. The picker also stops leaking prompts/formatters into the extraction-set chooser.
- **Test Step for API nodes.** The API node joins the task types you can run in isolation from the step editor, so you can debug a request — URL, headers, body, templating — without executing the whole workflow.
- **See each step's last-run output in the workflow editor.** A read-only "Last run output" panel in the step editor shows what a step produced on its most recent run (formatted output, task count, timestamp), so authors can inspect intermediate results without deleting downstream steps or re-running.
- **Editable workflow descriptions, surfaced in the library.** Workflow descriptions are now click-to-edit in the editor (mirroring the title) and shown as a one-line subtitle in the library list, so workflows are self-documenting.
- **Deployment version footer.** A version + environment footer in the sidebar makes deployments visually distinguishable; an optional `DEPLOYMENT_LABEL` setting (falling back to `ENVIRONMENT`) keeps two production deployments from both reading "production", surfaced via a new public `GET /api/config/version`.
- **Feature explainer pills on the Automations and Knowledge browse screens**, so "What are automations?" / "What are knowledge bases?" stays reachable once you already have items, not only from the empty state.
- **Search the admin Demo applications list** by name, title, email, or organization.
- **Creator credit on verified catalog items.** Approving a verification request now stamps a static credit — "by Jane Doe at University of Idaho" — onto the item's catalog metadata, built from the submission's name/org with the deployment's institution (`SystemConfig.org_name`) as the org fallback. The credit is plain text rather than a user reference, so it survives catalog export and seeding to installs where the creator has no account; the Explore/catalog cards fall back from the live submitter to the static credit. The catalog export embeds it, the seeder applies it from `_seed_meta.credit`, and `setup.sh` now asks for the institution name during install (stored as the branding org name when not already set). Ships with catalog **v1.1.1**, which credits the ten production workflows and the NIHAwardExtract template.

### Changed
- **Validation vocabulary is now consistent everywhere.** "Validate & improve" (the action) and "Optimized" (the result) replace the older internal "tune" / "autotune" / "autovalidate" labels across the knowledge-base, extraction, and workflow surfaces and the shared result tiles.
- **Module 6 of the certification is completable again.** The advanced-nodes grader had been checking for task names that never matched what the workflow editor actually stores, so the challenge could never pass. It now reads the editor's real node names and the lab is rebuilt around the Research node (no URL, key, or code required); Code Execution is documented as admin-only. Lesson 3 also drops "thread pool" jargon for plain language about parallel tasks.
- **The version indicator moved out of the always-on header into the account menu**, removing it from the page chrome while keeping it available on demand.
- **Dead/shadow UI removed.** A reachability audit found ~1,758 lines of unreachable frontend (superseded library/extraction panels, redirected pages, orphaned types); they were deleted and a `scripts/dead-code-scan.mjs` added for repeatable checks. No user-facing surface changed.

### Fixed
- **Uploaded documents were never vector-indexed, so KB and project chat retrieved nothing.** Two stacked bugs: per-user Chroma collections were named from the user's email, which contains `@`/`.` (illegal collection characters) and failed every embedding write; and semantic ingestion was dispatched fire-and-forget *racing* the extraction task that writes the document text, so it usually ran against empty text, produced zero chunks, and "succeeded" without retrying — leaving documents permanently unindexed (direct document chat still worked off raw text, which masked it). Collection names are now sanitized (and salted with a hash for uniqueness), and ingestion is chained to run only after extraction has written the text. Diagnosed in the field as 0 of 153 documents having any vector chunks.
- **Project chat could hallucinate document contents, and files moved into a project stayed invisible to it.** Empty KB retrieval was treated like "no KB requested" and fell through to a freeform answer; a dedicated project-KB prompt now refuses to invent project-document contents (while still answering general questions, clearly flagged as such). Moving a document or folder into a project now reindexes it into the project's KB (and removes it from the old one) via a background task.
- **Approval Gate workflows crashed** with "cannot prepare() more than once": the engine walked the dependency graph to execute and walked it again to locate the gate, exceeding graphlib's single-`static_order()` limit. The topological order is now memoized.
- **The admin Workflows search field rejected keystrokes** (its value was reset on every render); it now accepts input while keeping the debounced query.
- **Custom SSO provider labels were ignored.** The public auth-config endpoint read the wrong key, so the login button always fell back to a hardcoded label instead of the admin-configured display name.
- **KB test-query generation timed out as a 502 on larger KBs or slow models.** Generation ran the LLM call inline in the HTTP request, so a slow model or a transient connection blip outlived the proxy gateway timeout and surfaced as a 502. It now dispatches to a Celery worker and the UI polls a new `GET …/test-queries/generate/status` endpoint (mirroring the `add_urls` fix), returning immediately and reading the worker's result by uuid to avoid the incremental-insert race. The worker retries transient `ModelAPIError`/connection blips (10s/20s backoff) while letting genuine 4xx provider errors fail fast, and the status endpoint returns a clean, actionable message instead of leaking a raw exception repr.
- **Background LLM tasks leaked an HTTP client per run, exhausting file descriptors.** Each Celery task runs in a fresh event loop but only closed the loop, not the per-loop `httpx.AsyncClient` cached by `llm_service` — so every LLM-touching task leaked a client and its sockets until the worker hit `[Errno 24] Too many open files` and could no longer reach the model endpoint (surfacing as `ModelAPIError: Connection error.`). Exposed once test-query generation moved onto the worker. A shared `run_task_async` runner now releases the loop's pooled client before tearing the loop down, and the eight LLM-touching task modules route through it.
- **A removed or renamed LLM model gave one user a permanent "Connection error." in KB features.** KB test-query generation, validation judging, and optimization resolved the model from the user's stored selection verbatim; when that model was later deleted or renamed in System Config, the resolver found no endpoint/key and routed to the provider's public default host — which sealed deployments can't reach — a deterministic per-user failure while chat worked fine. These paths now validate the stored pick against the configured models and fall back to the system default (self-healing the stored value), matching chat.
- **KB validation and test-query answers filled empty retrieval from training data.** The RAG prompt on that path never told the model to refuse when retrieval came up empty, so out-of-scope questions got answered instead of a clean "not in the knowledge base." It now carries the same grounding rules as KB chat: treat retrieved chunks as possibly off-topic, refuse rather than guess when context lacks the answer, and prefix any general-knowledge supplementation with "Beyond the retrieved sources:".
- **The automations list issued one query per row to resolve linked workflow/extraction names** (an N+1 that dominated the request). Linked-target names are now batch-resolved in a single `$in` query per type; because the name is decorative, a resolution failure degrades that row to "unnamed" instead of 500-ing the whole `/api/automations` list.
- **Elapsed-time readouts during validation runs** no longer drift or freeze from client/server clock skew or missing UTC offsets, and validation/optimization runs honor cancellation mid-run.
- **Adding URLs to a knowledge base returned a bodyless 502.** `add_urls` awaited the full fetch-and-ingest inline — httpx + Playwright fallback, and up to 50 crawled child pages chunked and embedded serially — which routinely ran past nginx's 300s proxy timeout. The endpoint now sets the source to "building", dispatches the existing async ingestion to a Celery worker, and returns immediately with the queued count (the frontend was already built to poll status); a per-source failure now marks just that source as errored instead of failing the whole request.
- **Source citation pills in chat did nothing on tap and were invisible on touch devices.** The "Sources:" chips under an answer were hover-only `title` tooltips. They're now buttons that toggle an inline panel showing the cited chunk's preview, document title, and page/sheet, with active-state highlighting and `aria-expanded` for accessibility (the desktop hover tooltip is kept).
- **KB test queries and validation history needed a full page reload to appear.** The Validate panel fetched both lists only on mount, so wizard-generated test queries and a just-finished validation run never surfaced live. The Test Queries tab now refetches on entry, and the quality timeline takes a refresh nonce + polling flag so a completed run swaps into History in place without a reload or spinner flash.

### Operator Notes
- **Celery crashes are now observable.** Sentry is initialized in the Celery worker and beat entrypoints (not just the API process) via a shared `init_sentry()` helper, with `CeleryIntegration` and `monitor_beat_tasks` so unhandled task exceptions, soft-time-limit kills, and silently-stopped beat schedules report. Separately, the `engagement_tasks` and `retention_tasks` modules — scheduled in beat but never imported by the worker — are now registered, so data-retention and engagement jobs actually run.
- **Deployments self-report their build version.** `compose.yaml` passes a `VERSION` build-arg to the `api` and `celery` images, and `setup.sh` derives it (explicit env → version file → `git describe` → `dev`) at the `build_image` chokepoint, so `setup.sh`, `--redeploy`, and `--upgrade` all stamp the real version into the footer. An optional `DEPLOYMENT_LABEL` env var distinguishes multiple same-environment deployments. `./run_celery.sh` now resolves the `celery` binary for both `uv`-managed local checkouts and Docker images.
- **`setup.sh --redeploy` / `--upgrade` now prompt for the institution name when it is unset** (persisted to `SystemConfig`, never overwriting an existing value), closing the gap where install skipped it; non-interactive callers are never blocked.
- **This release carries verified-catalog v1.1.1** (creator credit on the ten production workflows and the NIHAwardExtract template) — an additive metadata update with no retirements. Apply it via `./setup.sh --upgrade` / `--seed` or Admin → Catalog.

## [v4.6.0] - 2026-06-10

> **Supersedes v4.5.0.** A `v4.5.0` tag was cut on 2026-06-04, but its release workflow failed during verification, so no images or GitHub release were ever published. Everything intended for v4.5.0 is included below; operators upgrade directly from v4.4.0 → v4.6.0.

### Added
- **Knowledge Base Query workflow node: grounded answers, dynamic queries, visible failures.** The KB Query node gains a mode toggle: **answer** mode retrieves passages and then synthesizes a grounded, citation-bearing answer via LLM (per-claim source labels, explicit "the passages don't contain this" behavior — the same retrieval-honesty rules KB chat uses); **passages** mode returns the raw chunks, now prefaced with a partial-excerpts framing header so downstream LLM steps treat them as evidence rather than complete documents. The query field supports `{{ inputs.output }}` templating (same engine as the API node), so a lookup can be driven by upstream step output — extract a sponsor name, then query that sponsor's policies. Retrieval results carry a 0–1 relevance score (shown as "% match" on citation chips) with an optional minimum-relevance gate, and every failure path — missing KB, empty query, retrieval error, zero hits — surfaces an amber step warning in the run output instead of silently passing empty context to the next step. New steps default to answer mode; existing saved steps keep returning passages.
- **In-app verified-catalog upgrades.** Admins see a banner/bell when the deployed code carries a newer catalog version than the database; a new admin tab runs the upgrade as a Celery task with live progress, including an opt-in **retire** pass that soft-archives verified items dropped from the catalog (reversible — underlying workflows/extractions/KBs are never deleted). Server-side, `seed_catalog --prune` performs the same retirement from the CLI.
- **Every LLM call is now metered.** A central wrapper + usage ledger records tokens for chat, UI extractions, and validation runs — previously only workflow runs recorded usage, so per-user/team token reports undercounted heavily.
- **"No Input" workflow input type** for workflows that run directly without selecting documents or other inputs.
- **Release tooling:** `scripts/cut_release.sh` (computes the next tag, refuses dirty/out-of-sync trees, pushes the annotated tag that triggers the release workflow) and `scripts/cut_catalog_release.py` (validates the seed tree, diffs new/refreshed/retired catalog items against a base ref, bumps `backend/seeds/VERSION`).
- **Present & Pitch communications surface.** A new public area under `/docs/present` for introducing Vandalizer to non-developers, visually distinct from the developer docs. Four audience tracks (leadership, IT/deploy, team, researchers), each rendered three ways from a single source-of-truth content file so they can't drift: a skimmable **Read** page, a full-screen keyboard-driven **Present** deck, and copyable ~30s spoken + one-paragraph written **Pitch** elevator pitches. Print-to-PDF emits a one-slide-per-page handout. Content is authored only from UI-reachable features with a claim→source map and a dev-time completeness guard. Entry points: a dedicated block in the Docs sidebar, an in-app link in the user menu, and an "Overview" link in the Landing top nav. Zero new dependencies.
- **Custom deployment branding.** Admins can set an org name and upload a logo (System Config → UI Theme). The values thread through the header, auth layout, footer, and chat panel via a new `BrandingProvider`/`useBranding` hook and the public theme endpoint. The footer keeps the "Powered by Vandalizer" attribution and NSF GRANTED acknowledgement whenever an override is in effect, so creator and funder lineage stays visible in open-source builds.
- **Per-user activity history drill-down for auditing.** Super-admins can open a single user's full activity as one merged feed (`GET /api/admin/users/{user_id}/history`) — the immutable AuditLog (who-did-what) and ActivityEvent telemetry (conversations / searches / workflows), newest-first, paginated. Each source caps at 1000 rows and flags `capped` (no silent truncation). The admin Users drill-down renders a timeline table (When / Source / Action / Resource / Status / IP) with a time-range selector, load-more, and an audit-trail CSV export (`/api/audit/export` now honors `actor_user_id`).
- **Setup readiness checklist and rich LLM connectivity diagnostics.** New `system_diagnostics` service: `diagnose_model()` runs a real round-trip and reports each step (config → protocol → endpoint → key → live call) — on success it explains why the hookup is healthy (protocol, endpoint, latency, tokens, the actual reply); on failure it classifies the provider error (auth / connection / model-not-found / timeout / rate-limit) into a plain-English cause + suggested fix. `build_readiness()` grades install setup into blocker/recommended items (a working LLM is a hard blocker; OCR and auth degrade softer). The admin config tab now renders an expandable step-by-step test panel instead of a tiny check/x icon, plus a SetupChecklist that auto-shows while a blocker is unresolved and deep-links to each fix.
- **Admin LLM Prompt Playground.** Super-admins can paste a system + user prompt, pick a configured model, and see the raw request and response side-by-side with latency and token counts. Errors are returned in-band so a 4xx/5xx from the upstream LLM renders next to the prompt that triggered it.
- **API Node templating.** The API node can now reference an upstream step's output in its URL, headers, and body via `{{ inputs.output }}` substitution with dotted-path access (dict keys and list indices), in both JSON-encoding mode (so `{"records": {{ inputs.output }}}` stays valid JSON) and raw-string mode. A blank body on POST/PUT/PATCH now sends the upstream output as the request body automatically. Adds a PATCH method option and inline syntax docs in the panel.
- **Inline credential creation in the API Node panel.** A "+ New" button next to the credential dropdown opens the full create form in a modal, pre-selected to match the chosen auth strategy; on save the dropdown refreshes and auto-selects the new credential — no navigation to the separate Credentials page required.
- **Support ticket type classification.** Requesters pick a type (bug / enhancement / feature request) when filing a ticket, surfaced end-to-end for triage: colored badges in the queue and ticket detail, plus a classification filter in the Support Center queue.
- **Test Step for Knowledge Base Query tasks.** KB Query was the one Test-Step-eligible task you couldn't run in isolation; since a KB query is a read-only vector lookup with no side effects, you can now test it and inspect the retrieved chunks without running a whole workflow.
- **Stop button to cancel a running workflow.** A new `POST /api/workflows/sessions/{session_id}/cancel` flips the run (and its activity event) to "canceled" and revokes/terminates the Celery task (SIGTERM), interrupting an in-flight step — including a long LLM call — rather than only the next step. The engine also checks a `should_cancel` callback cooperatively between steps for a clean terminal stop with no retry.
- **Verified KBs can be saved to a team, not just personally.** Adopting a verified KB from Explore now honors the requested `team_id` (omitted ⇒ private, current team ⇒ shared) and rejects a foreign team with 403; re-adopting moves the bookmark between personal and team. The Explore KB detail modal offers an explicit "Add to <Team> Knowledge Bases" action alongside the personal one.
- **Consistent name validation across user-created entities.** Workflows, folders, extractions, prompts, formatters, knowledge bases, automations, and library folders now share one rule (`utils/naming.py`): max 100 chars, trimmed, internal whitespace collapsed, control characters stripped, non-empty after normalization. Normal punctuation and unicode stay allowed. Replaces the prior unbounded-length names and the silent 300-char KB-title truncation users never saw.
- **Resizable Library sidebar.** The Saved Views / Folders sidebar (previously a cramped fixed width) now has a drag handle: width clamps to a sane range, persists to localStorage, and double-click resets to default. Drag suppresses text selection and cleans up its listeners.
- **Library kind-filter counts.** The Library filter chips (All / Workflows / Tasks) show per-kind item counts, and folder counts refresh after a delete.

### Changed
- **Trial lockout replaced with a friendly end-of-trial screen + self-serve renewal.** Expired trial users land on an explanatory screen with a renewal path instead of a hard lockout.
- **Present & Pitch link removed from the teams dropdown** (still reachable from the Docs sidebar, user menu, and Landing nav).
- **Knowledge Base chat now uses a dedicated, honestly-grounded prompt.** KB chat was reusing the document-chat prompt, which treats context as full documents — so the model filled gaps from training data while still citing the file that introduced the topic (answers looked grounded but weren't). The new `KB_CHAT_SYSTEM_PROMPT` names the retrieval reality up front, requires inline `[Source: filename]` citations that must match a snippet's Source line, demands a "_Beyond the retrieved sources:_" prefix for any general-knowledge fill-in, and instructs the model to say plainly when the snippets don't contain the answer. The in-context header now reads "Retrieved Knowledge Base Snippets" with a partial-excerpts caveat.
- **Workflow DOCX/PDF exports match the on-screen output.** The Word renderer gained real table support (GitHub-flavored markdown tables were being written as literal `| ... |` pipe text) and horizontal-rule support; both DOCX and PDF loosened the table-separator regex to the GFM minimum (one-or-more dashes, matching the `remark-gfm` the UI renders with), fixing the intermittent "hit or miss" PDF tables. Tables are themed to the brand palette.
- **Step Input tab explains the per-run, no-fan-out execution model.** Each step runs exactly once per workflow run with all selected inputs combined into a single call; this is now stated at the top of the Input sub-tab so users stop sending throwaway probe prompts to find out.
- **Pin / Favorite are clarified as item-wide.** When a library item lives in a folder, the Pin/Favorite tooltips and kebab labels now state the action applies to the item everywhere (it surfaces in the top-level Pinned/Favorites views), not just within the current folder.
- **Field validation and optional designations are preserved in workflow extraction.**
- **Module 4 certification grading counts standalone Extractions.** The grader previously collected fields only from Workflow Extraction tasks, but the challenge is built in the Extraction editor (a standalone `SearchSet`), so it undercounted — learners saw "8 unique fields" for a 26-field extraction. It now reads the user's own SearchSets directly and unions them with workflow-derived fields.
- **Verification submit consolidated onto the 4-step wizard.** The two submit UIs are merged onto `VerificationSubmitModal` (with the curated category dropdown and submitter org/name fields), used by library rows/details and the workflow + extraction editors. Workflow and extraction submissions now also show the same "Submitted for verification" success toast that KB submissions already did.
- **Support Center reply field auto-expands** for multi-line text (both the full-page agent view and the embedded chat panel), keeping Enter-to-send and Shift+Enter for newlines.
- **Confirmation dialogs added** before deleting a library folder (noting nested items move to root, not deleted) and before removing an adopted KB reference from My KBs.
- **Any team member can delete team-library items.** Deletion was gated at manage level but the Delete option was shown to every member and its failure swallowed, so a member's delete 404'd silently. Loosened to contribute level (personal stays owner-only, verified stays admin-only) with a failure toast.
- **Deploy docs reframed around `setup.sh`.** DEPLOY.md now presents the interactive wizard as the supported install path for both local evaluation and production (manual Docker Compose is the "escape hatch"), and the production `.env` block is labeled a reference for what the wizard writes. CLAUDE.md notes setup.sh as the supported deploy path.

### Fixed
- **Workflow KB citations never reached the Sources panel.** `MultiTaskNode` (which wraps every task in a real run) dropped the `retrieved_sources` emitted by KB Query steps, so the citations UI only worked in isolated Test Step runs. The wrapper now merges citations — and the new step warnings — through to the run result.
- **Recurring file-descriptor exhaustion from a leaked Redis pub/sub socket** in the browser-automation WebSocket path; the compose `nofile` ulimit was also raised as defense in depth (see Operator Notes).
- **Workflow input-type save failures were silently reverted** in the editor; they now surface an error instead of pretending to save.
- **Blank Formatter template box for pre-rename workflows** (older workflows stored the template under the legacy `prompt` key).
- **KB title not saving on blur.**
- **Zero-token "Connection error" in parallel workflow LLM calls.** Parallel-fanout steps (MultiTaskNode) intermittently failed after ~10s because non-external providers (Insight/Ollama/VLLM) fell back to pydantic-ai's process-wide cached HTTP client, whose connection pool was reused across the per-task event loops — raising a "bound to a different event loop" error the OpenAI SDK re-wrapped as a connection failure before any tokens were sent. Each `get_agent_model()` call now builds a dedicated `httpx.AsyncClient` bound to its own loop. Adds a configurable `workflow_llm_timeout_seconds` (default 120s).
- **Workflow approval-pause crash on non-BSON review artifacts.** Persisting a review artifact (e.g. bytes) could raise, freezing the run in "running". A new `_bson_safe()` coerces artifacts to BSON-encodable values before insert, and the extracted `_pause_for_approval()` marks the run "error" on any failure instead of leaving it stuck.
- **404 when running a deleted workflow.** If a workflow was deleted (or access lost) between opening the editor and clicking Run, the hook left `running=true` and leaked an unhandled rejection. It now resets state, toasts, and refreshes into the not-found view.
- **Admin panel 403 crash for examiners.** Examiners passed the frontend access gate but every admin analytics endpoint rejects them, and the default Usage tab's `Promise.all` had no `.catch()`, so the 403 surfaced as an uncaught rejection. Examiners are removed from admin-panel access (their workspace is `/verification`), email/teams tabs are hidden from team admins, and the Usage/Users/Teams/Workflows loaders now `.catch()` and show an inline error.
- **Uncaught "Request failed" rejections from on-mount fetches.** `useCertification` (mounted app-wide via `CertificationPanelProvider`) and `useLibraryItems` called their fetch inside `try/finally` with no `catch`, so a transient non-JSON 5xx escaped as a global unhandled rejection on every route load. Both now swallow the error and keep prior state; the next refresh recovers. Same hardening applied across other on-mount effects.
- **File-descriptor leak from per-call LLM HTTP and Redis clients.** Building a fresh client per LLM call / per task leaked FDs (toward the `Errno 24` exhaustion seen in production); clients are now reused. (Companion to the earlier per-call Mongo client fix.)
- **Support agents unable to see tickets.** Support-agent status was decided by an exact, case-sensitive match of the configured contact's `user_id` against the account `user_id`, so any mismatch (email vs username, casing, SAML/OAuth UPN) silently set `is_support_agent=false`. Centralized into `support_service.user_matches_support_contact`, matching on `user_id` or email case-insensitively.
- **Demo-user login failing for mixed-case emails.** Demo users were stored with a verbatim mixed-case email, but login lowercases the typed identity before a case-sensitive lookup — so the user got "We couldn't find an account" instead of "wrong password". The demo write paths now normalize email to lowercase; `scripts/normalize_user_emails.py` repairs existing records.
- **Team invites demoting an existing higher-ranked member.** `accept_invite` unconditionally overwrote an existing membership's role, so an owner/admin accepting a lower-role invite was silently demoted. It now only promotes, never demotes — mirroring `accept_join_link`.
- **API Node bodies dropped to zero bytes.** A configured POST/PUT/PATCH body that parsed to a JSON scalar or `null` hit a send path that only handled objects/arrays/strings and was transmitted as zero bytes (a confusing remote 400 with `body=''`). Non-object/array bodies now send their raw JSON text. The API node also defaults its Content-Type to `application/json` for JSON bodies.
- **Share-to-team errors are now diagnosable.** "Failed to clone the underlying workflow or extraction" collapsed two failure paths into one opaque code while the clone helper swallowed exceptions. A new `CloneSourceMissingError` surfaces a distinct `original_missing` (404) with an actionable "refresh the library" message; other clone failures are logged with a real trace.
- **Vanishing "Submit for Verification" dialog.** The modal rendered inline inside the library row's clickable div, so clicks inside it bubbled to the row's `onClick`, opened the editor, and unmounted the modal. It's now portaled to `document.body`.

### Security
- **Admin/credential secret fields no longer attract password-manager autofill.** A fresh user reported their login password populating the OCR API key field (a same-origin `type=password` input). Every admin/credential-vault password field is now hardened with `autoComplete=new-password` plus 1Password / LastPass / Bitwarden ignore hints so the major browsers and managers skip them.
- **Team invite / join-link acceptance is exempt from CSRF.** The accept POST fires immediately after a cross-site SSO redirect, the exact window where the freshly-set CSRF cookie can be briefly unreadable, producing spurious 403s (notably in Outlook/Defender Safe Links webviews). The exemption is safe and mirrors the existing SAML ACS exemption — the unguessable URL token is the actual authorization, so a forged cross-site POST gains nothing an attacker holding the token couldn't already do. Scoped to the accept sub-routes only.
- **Invite acceptance can only promote, never demote** an existing membership (see Fixed), closing a silent privilege-drop.

### Operator Notes
- **This release carries verified-catalog v1.1.0** (11 new items, 8 refreshed, 10 retired — a full sync to the maintainers' curated catalog). After upgrading, `./setup.sh --upgrade` / `--seed` (or Admin → Catalog) previews the 10 retirements (6 KBs, 4 extraction templates) and asks before soft-archiving them; declining keeps them and applies an additive-only update. Retirement is reversible and never touches items your users created or verified themselves.
- **This tag supersedes the unpublished v4.5.0.** No v4.5.0 images exist on GHCR; upgrade 4.4.0 → 4.6.0 directly. The v4.5.0 git tag has been removed.
- **`nofile` ulimits raised** in `compose.yaml` for api and celery: 8192 → 65536 (companion to the FD-leak fixes). Like the memory-limit change below, this requires recreating the containers: `docker compose up -d api celery` (or `./setup.sh --redeploy`).
- **Backend dependency ranges widened** (Dependabot): `aiosmtplib` <6, `redis` <9, `prometheus-fastapi-instrumentator` <9; `uv.lock` regenerated to match. Docker builds pick this up automatically.
- **Frontend dependency bumps since the v4.5.0 cut:** `@tanstack/react-router` → 1.170.15, `@tanstack/react-query` → 5.101.0, `@sentry/react` → 10.56.0, `react-dom` → 19.2.7, `typescript-eslint` → 8.60.1.
- **Container memory limits raised** in `compose.yaml`: api 4G → 8G, celery 8G → 12G. The api container was pinned at its 4G cgroup limit, so the kernel OOM-killed individual uvicorn workers; the master survived (container stayed "healthy") but nginx got connection-refused during each respawn and returned intermittent **502s** on `/api/*`. celery was OOM-reaped the same way during heavy document processing. **Apply with `docker compose up -d api celery`** — limit changes require recreating the container, not just a restart. (`./setup.sh --redeploy` does this for you.)
- **No new backend Python dependencies** this release. The Present & Pitch surface adds zero dependencies.
- **`scripts/normalize_user_emails.py`** is available to lowercase stored `email` on `User` / `DemoApplication` records if any mixed-case demo accounts predate the login fix. `user_id` is left untouched (it's a foreign key). Optional — only needed if affected accounts exist.
- **Frontend dependency bumps** (Dependabot): `@tanstack/react-router` 1.170.6 → 1.170.10, `@tanstack/react-query` 5.100.11 → 5.100.14, `@sentry/react` 10.53.1 → 10.54.0, `globals` 16.5.0 → 17.6.0, `typescript-eslint` 8.59.3 → 8.60.0. The Docker build picks these up; manual workflows must rerun `npm ci` in `frontend/`.
- **No drift** in `.env.example`, `README.md`, or `OPERATIONS.md` since v4.4.0. DEPLOY.md and CLAUDE.md were reworded to recommend `setup.sh` (no behavior change).

## [v4.4.0] - 2026-05-26

### Added
- **Shareable deep links for workflows, extractions, and knowledge bases.** A new `useShareLink` hook copies a `/?<kind>=<uuid>` URL to the clipboard from six entry points (LibraryItemRow + KBCard kebabs, the workflow / extraction editor headers, the ChatPanel KB banner, and the Explore detail modal). Workflow links now actually grant access: managers can mint a per-workflow `share_token` (`POST /api/workflows/{id}/share-token`); the recipient gets view-only access plus the existing "Make a copy" affordance, even without team or library membership. `ProtectedRoute` forwards the intended path to `/landing?next=` (validated as a same-origin relative path) so unauthenticated visitors can register on a 14-day trial and land directly on the shared item.
- **Support Center promoted further into a real agent workspace.**
  - **Internal notes**: agent-to-agent annotations on a ticket, never visible to the requester or non-agent watchers, no email blast on each note. Rendered as a yellow callout with a 5px solid left rule and a full-width "INTERNAL NOTE · Agents only" banner so agents stop mistaking them for replies.
  - **Watchers, ticket numbers, message editing**: each ticket gets a sequential `#1001`-style id from an atomic `SupportCounter` (legacy tickets backfilled in chronological order on first call), prefixed into every email subject. Owner, support, or an existing watcher can add a Vandalizer user as a watcher — read, reply, notify on changes. `SupportMessage` gains `edited_at` and a `PATCH /tickets/{u}/messages/{m}` endpoint with author-only authorization.
  - **Search, filter, multi-attachment management**: ticket number / subject / requester / message-body search (case-insensitive, regex-escaped), priority filter, Clear-filters affordance. Multi-file upload on existing tickets via repeated `files` form field, plus a `DELETE attachments` endpoint (uploader removes own, agents remove any).
- **Web fetcher service for URL ingestion.** All four URL ingestion sites (chat attachments, workflow Add Website node, KB URL sources, KB Celery ingest) now share `services/web_fetcher`, which runs trafilatura for main-content extraction and falls back to headless Chromium (Playwright) when the static fetch yields too little text — required for SPAs like uidaho.edu/policies. The chat URL-attachment prompt slice is raised to 80K chars now that content is clean text instead of raw HTML.
- **Custom names for KB sources.** Optional `custom_name` on `KnowledgeBaseSource` with a PATCH endpoint and inline rename UI. ChromaDB chunk metadata is rewritten on rename so retrieval citations track the user-chosen label, and the field is preserved through clone, export, and import.
- **Document filenames in workflow and KB chips.** The workflow step editor's "Select a Document" chip and the Knowledge Base sources list both fetch `SmartDocument` titles and render those instead of raw UUIDs (UUID surfaced as a hover tooltip). KB detail endpoint batch-resolves titles for document sources.
- **Document self-heal beat task.** New `tasks.document.reap_stuck` runs every 5 minutes to find documents stranded in `task_status="extracting"` (callers that dispatched extraction directly — the re-OCR script, the M365 attachment ingest path — used to leave docs with `raw_text` populated but status frozen) and dispatch `update_document_fields` to advance them to `complete`. Both direct callers also corrected to use `dispatch_upload_tasks` so the full pipeline runs.
- **Promote-to-full-user action in the admin Demo tab.** Trial users locked out at 14 days can now be converted to full users in one click (`POST /api/demo/admin/promote/{demo_uuid}`); previously the only recovery paths (release / restart-trial) kept them on the demo lifecycle.
- **Team library contribution for regular members.** Any team member can now add items to a team library; remove and update still require manage-level access. Closes the silent-404 case where members saw the team library in "Save to Library" but their adds quietly failed.
- **Remove-workflow-from-team-library action.** Workflow owners (or team admins) can unset `team_id` on a workflow, removing team access while keeping the creator's personal copy intact. Surfaced as a `UserMinus` row action with a confirmation modal that branches its copy depending on who is acting.
- **Shared-KB lifecycle decoupled from owner's My KBs delete.** Deleting a KB that is shared to a team now prompts the owner to either transfer it to the team (new `team_owned` flag — disappears from My KBs but stays accessible to the team) or unshare-and-delete everywhere. `DELETE` returns 409 for shared KBs when the `mode` parameter is omitted.
- **KB action discoverability**: Edit, Share/Unshare, Submit for Verification, Clone/Adopt, and Delete now live in a kebab menu on every KB card, matching the library-item pattern. Success/failure toasts wired up across every KB action (clone, add to My KBs, share, rename, adopt, remove reference, etc.). A pencil icon next to the editable KB title signals the click-to-rename affordance.
- **Verification authorship**: submitter name now surfaces across verification management (catalog cards, queue rows, collection item lists). The "by author" chip falls back to `created_by` when the verified item has no `VerificationRequest` (covers admin-direct verifies and originally-seeded items). Works across workflows, search sets, and knowledge bases.
- **Frontend Sentry source-map upload.** The frontend Docker build now wires `@sentry/vite-plugin` and, when `SENTRY_AUTH_TOKEN` is set, emits source maps, uploads them against `VITE_SENTRY_RELEASE`, and strips the `.map` files from `dist/` before the nginx image is built so nothing is served publicly. Local builds without the token produce byte-identical output to before.
- **Auto-reload on CSRF validation failure**: a 403 with detail `CSRF validation failed` triggers a one-shot `window.location.reload()` guarded by a per-tab sessionStorage flag, healing stale SPA bundles cached from before the `__Host-` cookie rollout. The flag prevents an infinite reload loop if the cause is something a reload can't fix.

### Changed
- **CSRF cookie hardened to `__Host-csrf_token` with `SameSite=Lax`.** The browser-enforced `__Host-` prefix refuses any cookie of that name without `Secure`, `Path=/`, and no `Domain`, closing a failure class where duplicate `csrf_token` cookies (sibling app on a parent domain, prior deploy with a different Path, proxy) collapsed differently in Python's `SimpleCookie` (last-write-wins) versus the JS regex (first match), 403ing every POST. The middleware validates the `X-CSRF-Token` against the full multiset of legacy values present in the raw `Cookie` header during the transition so old and new SPAs both pass; modern `__Host-` cookies remain duplicate-immune by browser policy. `SameSite=Lax` replaces `Strict` to fix the Chrome quirk where a freshly-set CSRF cookie on an OAuth callback response wasn't readable on the immediate follow-up, breaking invite-accept POSTs after Azure sign-in. `/api/auth/saml/` is exempt from CSRF (the ACS POST is cross-site from the IdP and authenticated by the signed assertion). Development (HTTP) keeps the legacy cookie name since `__Host-` requires `Secure`.
- **Workflow share links now grant access** (previously: backend only honored team-membership / library access, so recipients hit 404 despite the UI promising "share with anyone"). Service layer falls back to `share_token` when team/library checks fail; view-only recipients see the existing "Make a copy" flow.
- **Activity rail title shimmer resolves on every exit path.** `generate_activity_description_task` used to only set `description_generated=True` on success, so no-context / no-model / empty-output / exception branches sat shimmering until the frontend's 2-minute fallback timer fired. Now hoists `db` out of the try block and uses a `_mark_done()` helper that sets the flag on every exit (with title + `ai_description` on success).
- **Chat panel clears when its activity is deleted.** ActivityRail mirrors the active conversation through workspace context and triggers a fresh chat when the deleted activity matches; previously the messages stayed on screen because useChat's `conversationUuid`/`messages` had no signal that the backing conversation was gone.
- **KB upload UX**: "Supported: pdf, doc, docx, ..." caption and a matching `accept` attribute on the file input; client-side pre-validation in `handleFiles` so dropped `.png`/`.exe` fail instantly instead of after a backend round-trip; a remove (x) button on each non-uploading row; upload rows keyed by stable id so removals patch the right entry.
- **Author chip on the Explore item detail modal** gets an `on-dark` tone variant (white on translucent white) so it reads against the modal header's purple/teal/sky gradient — previously low-contrast "muddy brown on purple".
- **Right-align numeric headers** in the Admin usage stats tables to match the right-aligned values.
- **Library / KB**: stranded item author falls back to creator across verified search sets and KBs (not just workflows). Workflow / library status polling for library-launched workflows fixed (see Fixed).

### Fixed
- **Chat URL attachments truncating to page header (#447).** Two compounding bugs caused the LLM to receive only HTML `<head>` metadata when users attached a link in chat: `routers/chat.py` stored the raw HTML with no extraction; `chat_service.py` then sliced the raw HTML to `[:10000]`, which on Next.js / SPA pages is entirely `<head>`, scripts, and Tailwind classes. Replaced by the shared `web_fetcher` service (trafilatura + headless-Chromium fallback) at all four URL ingestion sites.
- **Documents stranded in `task_status="extracting"`** rendering as "Reading text..." forever in the frontend (fix described in Added — `reap_stuck` beat + corrected direct callers).
- **404 polling status for library-launched workflows.** `/run` accepted library-backed access via `get_authorized_workflow`, but `/status` and `/batch-status` used bare `can_view_workflow`, so users polling a verified workflow they launched from the library got 404 forever and the UI hung in "running".
- **Global drop guard cancelling in-app reorder drags (#436).** `useGlobalDropPrevention` was set up to block OS files dropped outside a drop zone but ran for every drag, setting `dropEffect='none'` on `dragover`. In-app row handlers (extraction reorder, admin org tree) don't `stopPropagation`, so the global listener fired on bubble and the drop was silently cancelled. Now filters on `dataTransfer.types` including `'Files'`.
- **Unverify action 404**: frontend was calling `POST /api/verification/verified/{kind}/{id}/unverify` but the backend exposes `DELETE /api/verification/verified/{kind}/{id}`.
- **`workflow_share_token` missing from `Navigate` calls.** The search schema added the field but the literal search objects in `Navigate`/`navigate`/`Link` callsites weren't updated, breaking the typecheck and Docker build.
- **`SupportCenter` build typecheck collision.** A `useState<string>('search')` debounced search box collided with the existing `useSearch()` URL-params binding also named `search`, producing redeclaration errors and two cascades into `listTickets()` / `ListView.activeSearch`. The `useSearch()` result is renamed to `urlSearch`.
- **Frontend lockfile @emnapi optional deps.** `npm ci` was failing in Docker builds because `@emnapi/core` / `@emnapi/runtime` were referenced as deps but missing their own `node_modules` entries — npm's known cross-platform optional-dependency lockfile bug. Lockfile regenerated to pick up the Linux-only entries.
- **KB URL ingest paging Sentry on unreachable hosts.** `ValueError` from `validate_outbound_url` (bad scheme / unresolvable host / private IP) and `httpx.RequestError` (connect/timeout/read errors) are now demoted to warnings in both URL-ingest paths. The source is still marked `status=error` with the message surfaced in the UI; the Celery task stops re-raising since `ValueError` was never in `TRANSIENT_EXCEPTIONS`.
- **Trafilatura library `ERROR` log noise** silenced in `web_fetcher` (empty / unparseable HTML is already handled by the BeautifulSoup fallback). `"Not authenticated"` and `"Rendering cancelled"` added to the frontend Sentry `ignoreErrors`.
- **CSRF errors on multipart uploads.** Several raw `fetch` callsites (support tickets, document uploads, workflow / extraction imports, chat streaming) bypassed `apiFetch` and its 403 self-heal, so users with stale CSRF cookies saw hard errors on those endpoints while JSON requests recovered silently. Centralized through a new `rawFetch` helper; the middleware also logs structured fields on 403 to diagnose remaining reports.

### Security
- **CSRF cookie unspoofable by collision.** The `__Host-csrf_token` prefix is browser-enforced (Chrome refuses to store a cookie of that name without `Secure`, `Path=/`, no `Domain`), so a sibling app on a parent domain or a prior deploy with a different `Path` cannot shadow Vandalizer's token. Header validation against the full multiset of legacy values prevents bypass during the rollout window.
- **Workflow share tokens** grant view-only access only. `can_manage` is forced false for share-token-authenticated recipients, and they see the existing "Make a copy" flow rather than write paths.
- **Author-only message editing**: even support agents can't rewrite someone else's `SupportMessage`.

### Operator Notes
- **Two new backend Python dependencies** for the web fetcher: `trafilatura>=1.12,<3` and `playwright>=1.49,<2`. The backend Dockerfile installs the Chromium binary via `playwright install --with-deps`, growing the backend image by ~400MB. Opt out with the env var `WEB_FETCHER_BROWSER_ENABLED=false` (static fetch only — chat / KB / workflow URL ingestion on SPAs will degrade to whatever trafilatura can extract from initial HTML, which may be empty). `./setup.sh --redeploy` and `docker compose up -d --build` pick the new deps up automatically via `uv sync`. Manual installs must rerun `uv sync` in `backend/` **and** run `playwright install --with-deps` against the venv.
- **`langchain-text-splitters` constraint widened** from `>=0.3,<1` to `>=0.3,<2` (the dependency hit a `1.x` major). No code change required; bumped via Dependabot.
- **New Celery beat job**: `tasks.document.reap_stuck` runs every 5 minutes to advance documents stuck in `task_status="extracting"`. No operator action — runs out of the standard worker pool.
- **New frontend Docker build args** wired in `compose.yaml` and `frontend/Dockerfile`: `VITE_SENTRY_DSN`, `VITE_SENTRY_ENVIRONMENT`, `VITE_SENTRY_RELEASE`. `deploy.sh` writes a top-level `.env` so every server's `docker compose build` sees the DSN automatically on first deploy and on every redeploy — no manual step. Existing deploys with a DSN already in `backend/.env` will start reporting frontend errors after the next image build.
- **Optional GitHub Actions secrets/vars for Sentry source-map upload**: `SENTRY_AUTH_TOKEN` (secret), `SENTRY_ORG` (var or secret), `SENTRY_PROJECT` (var or secret). If unset, the build skips source-map upload — behavior is unchanged. Once set, the next `sha-tagged` and `v*.*.*` build will publish maps and `.map` files are deleted from `dist/` before the nginx image is built.
- **CSRF cookie name migration to `__Host-csrf_token`** is transparent. The middleware accepts the legacy `csrf_token` cookie during the transition; stale SPA tabs from before this release will auto-reload once on their next 403 thanks to the new client-side handler.
- **Frontend dependency bumps**: `typescript` 5.9.3 → 6.0.3, `@tanstack/react-router` 1.169.2 → 1.170.6, `@tanstack/react-query` 5.100.9 → 5.100.11, `marked` 18.0.3 → 18.0.4, `@vitest/coverage-v8` 4.1.5 → 4.1.7. Build via Dockerfile picks these up; manual workflows must rerun `npm ci` in `frontend/`.
- **No drift** in `.env.example`, `README.md`, `DEPLOY.md`, or `OPERATIONS.md` since v4.3.0.

## [v4.3.0] - 2026-05-15

### Added
- **Workflow approval / review system, end-to-end.** The Approval node is now reachable from the editor with a full config panel (review instructions, assignee role, SLA in days, timeout action, escalation reviewers). New `/reviews` inbox with My / Team queues, status filter, and type-aware artifact rendering (text, markdown, JSON, extraction table, document download). Reviewers can edit text / markdown / JSON / single-row tables before approving — the edited artifact flows back into downstream workflow steps. Assignee roles: `workflow_owner`, `team_admins`, `specific_users`. SLA enforcement runs on a 5-minute Celery beat sweep (`tasks.approvals.expire_overdue`) that fires the configured timeout action (auto-approve / auto-reject / escalate / mark expired). `/api/approvals` is replaced by `/api/reviews`; the old `/approvals` route redirects.
- **Scoped management API at `/api/mgmt/v1`** for service consumers and agentic tools. New `ApiKey` model with named, scoped, revocable keys (`vk_live_…`), per-key rate limiting, and audit-log entries on every call. Read endpoints cover stats, users, teams, workflows, documents, activity, audit, config (secrets redacted), and the full validation domain (runs, test cases, plans, cross-field rules). Write endpoints support test cases (incl. bulk upload), cross-field rules, and validation plans. Run endpoints (validation, workflows, extractions) gated on separate scopes since they spend tokens. Issuance + revocation now open to all staff via the in-app API Keys tab, with `docs/mgmt-api.md` linked alongside.
- **Convert-to-Knowledge-Base flow for oversized documents.** Workflow and chat now pre-flight document size against the model's input budget; oversized refs trigger a structured error with an inline "Convert to Knowledge Base" action that wraps the docs in a fresh KB and activates it in workspace context. Page-level (PDF) and per-sheet (XLSX) citation offsets are now generated end-to-end, including for OCR-routed PDFs. Document ingest writes `chromadb_ready` / `chunk_count` / `ingest_error`; the file browser shows a distinct amber icon for retrieval-broken docs (separate from upload-validation failures).
- **LLM endpoint context-window probe.** System Config → Models gains a "Context Window" field and a "Probe endpoint" button that queries the configured endpoint for its real serving limit — `max_model_len` (vLLM), `context_length` (OpenRouter), or `model_info.<arch>.context_length` (Ollama). Anthropic and plain-OpenAI endpoints that do not expose the value report that, so admins know to fill it in by hand. The chat and workflow context-budget planner now trims against the real number instead of an inflated substring-table default, so compaction no longer undertrims into a mid-workflow `400`.
- **Credentials store + OAuth `client_credentials` on API Node.** New team-scoped `Credential` collection with per-field Fernet encryption and Redis-cached bearer tokens. API Node grows an `auth_strategy` selector (`none` / `static_header` / `oauth_client_credentials`). Secrets never round-trip back to the client (redacted as `"<set>"`). New Credentials management page and credential picker in the API Node config panel.
- **Public team join links.** Owners and admins can mint shareable join URLs that bypass the trial waitlist; invite acceptance has a proper OAuth + redirect flow.
- **Workflow output as chainable artifacts.** Run output saves as a `SmartDocument` that downstream steps can ingest, or as a real file in a SmartFolder of the user's choosing. Markdown output renders as a beautifully formatted PDF (with Unicode-hyphen normalization to avoid encoding crashes), a real `.docx` is generated alongside, and multi-step deliverables bundle as a ZIP. Download filenames are now unique per session.
- **Retention and classification dashboards in the Admin panel**, plus a Retention Policy admin UI for configuring rules.
- **Team notifications when content is shared** (bell + email). Activity rail auto-fails items stuck in `processing` so users do not stare at zombie spinners.
- **LLM tasks can combine multiple input sources** in a single step (previously: one source per task).
- **Auto-clone from Explore** when running a verified workflow, with linked extraction fields preserved.
- **Workflow editor polish**: "Add selected documents" shortcut for fixed-doc inputs, hover tooltips with descriptions on each task type in the picker, explainer overlays in empty Automations and Knowledge Bases tabs, Combined-vs-per-doc tooltips on Run, original author shown on workflow cards (Workflows, Library, Explore).
- **Confirm-before-delete** dialogs across workflows, extractions, Library panel, and the workspace Library tab. File-browser bulk selection clears even when a delete throws.
- **API Keys tab** offers a downloadable Claude Code skill for users wired up against the mgmt API.
- **OCR rerun script** (`scripts/re_ocr_old_documents.py`) to re-OCR documents older than N days.
- **Workflow JSON upload-from-creation-modal** entry point.
- **Test Step** is now honest about failures and its (limited) scope.
- **Certification panel** is mounted app-wide so it opens from any page; the Foundations lesson no longer misroutes users below the action button.
- **Trial check-in tickets** move out of Support Center into the Demo tab.
- **`setup.sh` menu restructured** with a catalog reset entry.

### Changed
- **M365 config setup is replaced by a Document Compliance activation flow** in the admin UI. The new path is the supported way to wire Microsoft Graph integration; the old config-driven setup is gone.
- **Workflow output "save" semantics:** "Save to folder" now means a SmartFolder file in the user's folder tree, not a Library bookmark. Add-to-Library dialog is trimmed from Explore; the dialog now drops the unmanaged note/tags fields and splits share errors so the user can tell what failed.
- **Verified items save to the Library by reference**, not by silently cloning the underlying document.
- **Workflow runner treats `pending_approval` as paused** (not running), so polling state matches what the user sees.
- **Workflow activity-rail polling resumes** when the user opens a running workflow from the rail.
- **Any team member** can now share items and create folders in the team Library (previously: owners/admins only).
- **API Node** surfaces header JSON parse errors instead of silently dropping them; OAuth-authenticated API Node calls use the new credentials store rather than inline static headers.
- **Login error messages** now explain why a login was rejected (locked, unverified, bad credentials) instead of a generic "Invalid credentials".
- **Workflow run button** muted state shows a "Select a document to run" hint; clearer errors when adding a workflow task fails.
- **Activity rail** shimmers in AI-generated titles as they stream in.
- **Library / Explore UI polish**: "more options" icon unified across file browser and library; verified-catalog "system" author renders as "Verified Catalog"; library row title takes full width with actions floating over `last used` on hover; same for file/folder rows; Library type filter "All Types" shortened to "All"; chat input restored to full width; assistant chat constrained to match input width; in-progress KB convert button dropped from the workflow editor panel; Add Document task search boxes now surface files; API Key scopes picker has Select all / Clear shortcuts.
- **Workflow / extraction inputs** can be uploaded from a JSON file at creation time; "Add selected documents" works from the library context menu.
- **Extraction validations decoupled from their source documents** — validation plans persist independently and can be downloaded as a zip of the setup. Extraction Tools tab orders import/export at the top; saved sets no longer silently drop manual extraction fields.
- **XLSX document viewer** renders formulas; markdown step output explains how input sources combine.
- **Extract API uploads default to ephemeral cleanup** so test runs don't litter the workspace.
- **Pause/resume flow**: `ApprovalNode` honors the new approval record fields; the workflow editor pending-approval banner links to the full review screen.
- **Module 4 certification content** renames "enum values" to "Allowed values"; cert module stays on the Challenge tab after completion.
- **`seed_catalog`** behavior already shipped in v4.2.0; this release continues the catalog reset entry in `setup.sh`.
- **System Config sticky header** gets horizontal padding so the gear icon and Save button no longer hug the panel edges.

### Fixed
- **Doubled workflow LLM latency** from cross-event-loop agent caches.
- **Workflow PDF crash** on triple-asterisk markdown sequences.
- **Folder-watch automations** dropping storage + notifications.
- **`/api/mgmt/v1/stats`** choking on legacy stub `SmartDocument` records.
- **Upload validation crash** when `langchain-text-splitters` was not installed (dep is now declared in `pyproject.toml`).
- **Approval flow** leaving the existing `LibraryItem` unverified after an approve.
- **Convert-to-KB setter calls** for non-Dispatch workspaces.
- **Invisible role dropdown options** on trial signup.
- **Re-OCR script** for old documents (operator tooling).
- **Pending-task leaks** from custom HTTP middlewares (`SecurityHeadersMiddleware` rewritten as pure ASGI middleware so client disconnects mid-response don't strand asyncio tasks).
- **Switching an automation's trigger type back to `folder_watch`** no longer errors.
- **Activity-rail items stuck in `processing`** auto-fail after the watchdog window instead of hanging the rail.
- **"Documents ready for analysis" banner, file-list polling, and the row spinner** flipped to "ready" the moment text extraction finished, while RAG indexing was still running in the background. All three now key off `isDocReady` and treat the `readying` / `extracting` task states as not-yet-ready, so the UI stays honest across the full upload pipeline.
- **Silent OCR / extraction failures** (image-only PDFs, OCR endpoint down, encrypted files) were recorded as a successful `complete` with empty text — the extracted-text modal rendered blank and chat silently skipped the document. Extraction now marks the document errored with a specific reason, `poll_status` surfaces it, and the modal offers a **Retry extraction** button that re-dispatches the upload pipeline.
- **Star / set-default button on System Config models** — `PUT /config/models/default` was registered after `PUT /config/models/{index}`, so FastAPI tried to parse `default` as an integer index and rejected the request. The literal route is now registered first.
- **Workflow pre-flight oversize check** read `llm_models` from `SystemConfig` when the field is `available_models`, so any `context_window` override was silently ignored.
- **Frontend errors never reached Sentry.** `VITE_SENTRY_DSN` is a Vite compile-time variable but was missing from every Docker build path, so `initSentry()` short-circuited in production and the Sentry dashboard stayed empty. The DSN, environment, and release are now passed as build args through `frontend/Dockerfile`, the Makefile `docker-build` target, and the release / build-container GitHub workflows.

### Security
- **Default extract-API uploads to ephemeral cleanup** so transient uploads do not persist in shared storage.
- **Mgmt API keys**: per-key scope, per-key rate limit, audit log on every call; secrets redacted in all read responses.
- **Credentials store** uses per-field Fernet encryption; `payload` is never echoed back to clients.

### Operator Notes
- **New backend Python dependency**: `langchain-text-splitters>=0.3,<1`. `./setup.sh --redeploy` and `docker compose up -d --build` pick it up automatically via `uv sync`. Manual installs must rerun `uv sync` in `backend/`.
- **New Celery beat job**: `tasks.approvals.expire_overdue` runs every 5 minutes to expire SLA-overdue reviews. No operator action — runs out of the standard worker pool.
- **Three new routers** mounted: `credentials` (`/api/credentials`), `reviews` (`/api/reviews`), `mgmt` (`/api/mgmt/v1`). The old `/api/approvals` is removed; the frontend `/approvals` URL redirects to `/reviews`.
- **New GitHub Actions secret for frontend error reporting**: add `VITE_SENTRY_DSN` as a repository secret (Settings → Secrets and variables → Actions) so released frontend images report to Sentry. `build-container.yaml` also reads an optional `vars.VITE_SENTRY_ENVIRONMENT` repository variable to label non-tag builds (e.g. "staging"). This is a CI-only setting — no `.env` or `compose.yaml` change.
- **No drift** in `.env.example`, `compose.yaml`, `DEPLOY.md`, `OPERATIONS.md`, or `README.md` since v4.2.0.

## [v4.2.0] - 2026-05-04

### Added
- Native `anthropic` and `openrouter` protocol options in System Config → Models. Anthropic uses pydantic-ai's `AnthropicModel` for first-class Messages API / native thinking / tool use. OpenRouter uses `OpenRouterProvider` with default `https://openrouter.ai/api/v1` and `Vandalizer` app attribution; honors a custom endpoint for self-hosted gateways. `claude-*` model names still auto-detect to `openai` for back-compat — opt in to native Anthropic by selecting it explicitly in the dropdown.
- Admins can now pick the default LLM model from System Config
- Cmd/Ctrl+F find-in-document search for PDF, DOCX, and spreadsheet viewers
- Knowledge base export and import in the UI; file uploads and folder filtering when adding to a KB
- Workflow JSON import on the Workflows page, with a toast on import success
- LLM-powered "Improve" button in the Prompt task editor
- Real Word (.docx) download for workflow results; multi-step workflow deliverables bundled as ZIP at download
- API tab on the extraction editor with ready-to-copy curl and Python snippets; `/extractions/run-integrated` accepts raw text input
- Context-budget planner and compaction for chat requests; auto-grow chat input textarea with highlight focus ring; uploaded documents attach to the chat context immediately
- Admin Certifications panel showing user progress with a debug unlock; fullscreen mode in the certification panel
- Demo program admin: Applications / Surveys subtabs, plus `credentials_sent_at` and `last_login_at` tracking in CSV export
- Analytics: time range extended to 2 years and CSV export coverage broadened
- Support Center promoted to a true agent workspace under the Teams dropdown — agent ticket filing, support-agent tags, default open filter, attachments on ticket creation, shareable ticket URLs, and email notifications to other agents on tag changes
- In-app trial check-in card for users approaching trial expiration
- `setup.sh`: cron-based auto-update option, auto-prompt for upgrade when running an outdated version, and code + catalog versions shown on setup with `Scan & upgrade` replacing `Upgrade`
- `seed_catalog`: `--only` flag and upsert semantics for safer reseeding
- `docs/api.md` external API reference, linked from the README
- Backend test coverage: +11 test files in the first installment, plus tightened CI gates for stability

### Changed
- `compose.yaml`: backend and Celery services now set `nofile` ulimits to 8192 soft/hard. **Operator action: rebuild and restart the stack** (`./setup.sh --redeploy` or `docker compose up -d`) to pick up the new limits — required to avoid `EMFILE` under heavy KB ingest load
- `DEPLOY.md`: Models section now documents the `anthropic` and `openrouter` protocols alongside `openai`/`ollama`/`vllm`
- KB ingest pipeline shares a single ONNX embedder and Chroma client to avoid file-descriptor exhaustion
- Compact extraction editor tabs with icons and responsive collapse; the API tab is folded into Advanced
- PDFs open inline in a new tab instead of triggering a download
- Document validation surfaces its reason in a tooltip on the file warning icon
- Workflow run button shows a "Select a document to run" hint when muted; clearer errors when adding a workflow task fails
- Folder breadcrumb navigation made more discoverable; file-browser checkbox hit target now spans the whole cell
- Library and extraction list sidebars refresh their cache after a workflow or extraction import
- Move Support Center to the Teams dropdown for support agents; support notifications open the ticket directly, and ticket clicks open in the chat panel
- Removed em dashes and double em dashes from user-visible UI text and messages
- Removed the admin "Debugging" tab from the Admin panel
- Pass document text into `ResearchNode` and `FormFillerNode` on a Document trigger
- Aggregate extraction fields across all tasks in certification validators
- Send a set-password email to SSO-only users who hit "forgot password"
- Respect `thinking=false` in LLM requests; route Qwen thinking toggles through `chat_template_kwargs`
- Deeper XLSX, DOCX, and PDF extraction tuned for research-admin documents; CSV parser and OCR improvements
- Sanitize PDF download text for fpdf core-font latin-1 encoding
- Allow `.txt` / `.md` files through upload validation and the secondary picker
- Capture selected docs when launching a prompt task from the library
- Library: surface unprocessed chat docs; fix `last used` timezone, sorting, and highlighting; "Move to folder" label no longer opens the item
- Stop long document titles from squeezing out the reveal-markdown button
- Portal the verified-workflow preview modal to escape its panel stacking context
- Make Import Definition replace the open workflow; Advanced tab UI cleanup
- Move extraction import/export cards to the top of the Tools tab; import extraction definitions into the open SearchSet

### Fixed
- `EMFILE: too many open files` errors on KB ingest under load
- KB collection deletion is now idempotent when the collection is already absent
- Endless spinner and missing OAuth flow on team-invite acceptance
- Workflow Document-trigger input handling and Input-tab drop zone
- Automation wizard "Importing a module script failed" error
- "Add Document" task search boxes not surfacing files
- `500` on `GET /api/workflows/{id}` when dict fields held raw ObjectIds
- Library share crash from an invalid `SearchSetItem.space_id` access
- `admin.py` use of `get_agent_model`
- Chat: persist a placeholder assistant turn when the stream fails so the conversation does not appear to silently drop
- Several unresolved Sentry errors in `vandalizer-backend`
- Clear the extraction-result highlight when the viewed document changes

### Security
- Gate automation editing on the `can_manage` permission so non-managers cannot mutate automation configs
- Only notify configured support contacts on new tickets and messages, not all admins

## [v4.1.0] - 2026-04-20

### Added
- Docker Compose stack with healthchecks for all services (Redis, MongoDB, ChromaDB, API, Celery, frontend)
- `backend/Dockerfile` — multi-stage build with non-root user and healthcheck
- `frontend/Dockerfile` — Node build stage with nginx runtime
- `frontend/nginx.conf` — SPA routing with API proxy and security headers
- GitHub Actions CI pipeline (`ci.yaml`) — runs pytest and TypeScript checks on every PR
- GitHub issue templates (bug report, feature request) and PR template
- Dependabot configuration for pip, npm, and GitHub Actions
- Rate limiting on auth endpoints (login, register, refresh) via slowapi
- Security headers middleware (X-Content-Type-Options, X-Frame-Options, Referrer-Policy)
- DOMPurify sanitization on all markdown-rendered HTML in the frontend
- Backend test suite: config validation, JWT tokens, file validation, health endpoint smoke test
- `CODE_OF_CONDUCT.md` referencing Contributor Covenant v2.1
- `CHANGELOG.md`
- Root `Makefile` with canonical backend, frontend, and release validation targets
- Tagged GitHub release workflow that reruns release validation, publishes versioned GHCR images, and creates a GitHub release
- `RELEASE_CHECKLIST.md` covering changelog curation, release validation, bootstrap smoke checks, and rollback readiness

### Changed
- Tightened CORS to explicit methods and headers instead of wildcards
- `insight_endpoint` default changed from UIdaho-specific URL to empty string
- Celery hardened with task time limits (30 min soft / 31 min hard) and result expiry (24h)
- `CodeExecutionNode` now runs sandboxed code in a killable child process so timeout cases do not hang the worker or test process
- `compose.yaml` fully rewritten for the FastAPI + React stack
- `.dockerignore` expanded to exclude deprecated code, secrets, and build artifacts
- README quickstart updated with Docker Compose as the recommended path
- `CONTRIBUTING.md` rewritten with correct FastAPI paths and commands
- `DEPLOY.md` now includes a Quick Start with Docker Compose section
- `.env.example` files updated with comments and missing variables
- Backend dev tooling is now installed deterministically via `uv sync --frozen --extra dev`, without ad hoc CI `pip install` steps
- CI now uses shared `make` targets so local, PR, and release checks run the same commands
- Continuous image workflows now use sanitized branch tags plus explicit `sha-<short>` image tags
- Fixed a frontend production-build type regression in `ExtractionEditorPanel` so local and Docker builds pass again
- Fixed workflow approval pause propagation so `ApprovalNode` pauses execution even when wrapped in `MultiTaskNode`
- API-triggered automations now authorize caller-supplied existing document UUIDs before workflow or extraction execution
- Chat resume, add-link, and add-document routes now require the referenced activity to belong to the current user before reusing or mutating activity/conversation state
- Browser-automation session creation now verifies that the referenced workflow result belongs to a workflow visible to the current user before opening a session
- Knowledge-base suggestion creation now requires visibility on the target KB, and suggestion review is bound to the KB in the route instead of trusting a bare suggestion UUID
- Knowledge-base cloning now reuses the same KB visibility checks as the route layer instead of bypassing org/team scoping with a raw KB lookup
- Team-scoped admin analytics and workflow views now normalize mixed team UUID/ObjectId history, including same-team drill-downs opened by team UUID
- Extraction status polling now resolves caller-supplied activity IDs through `PydanticObjectId`, keeping API-key lookups aligned with the owned-activity path
- Added governance-route coverage for document classification and admin-only retention holds across personal, team, outsider, and admin cases
- Updated stale backend config/auth tests to match the production `openai_api_key` requirement
- Split backend release-gating tests from the current backend static-analysis backlog via `make backend-ci` and `make backend-static`
- The canonical `bootstrap_install.py` entrypoint is now covered directly in the backend test suite, not only through helper-level script tests
- Fix broken 'Test model' button in Admin Config UI

### Security
- JWT secret validation: app refuses to start with the default `change-me` secret in non-development environments
- NoSQL injection fix: `re.escape()` applied to all `$regex` search parameters in admin routes
- XSS prevention: all `dangerouslySetInnerHTML` usage now wrapped with `DOMPurify.sanitize()`

### Removed
- Hardcoded UIdaho `insight_endpoint` default
