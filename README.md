Document review using LLM models

Wee!
 

## Installation

### Necessary Secrets

## Testing

We have a few End To End Tests written in Pytest and Selenium.

They are configured to run against `https://vandalizer-dev.nkn.uidaho.edu` with `tox` using `tox run` in a headless browser.

Alternately, they can be run locally with `pytest`.